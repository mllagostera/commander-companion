<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".CommanderCompanionApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommanderCompanion"
        tools:targetApi="31">

        <!-- The language picked in Settings is applied and persisted through this service, and
             it has to be declared here by hand.

             `autoStoreLocales` used to sit directly under <application>, where AppCompat never
             looks for it, and this service was not declared at all. The effect was that
             AppCompatDelegate.setApplicationLocales() had no Context to reach the platform
             LocaleManager with (MainActivity is a plain ComponentActivity, so AppCompat is
             never initialized by an AppCompatActivity either): the call kept the value in
             memory and applied nothing. The language selector looked like it worked only
             because the chip was driven from the ViewModel's own copy of the choice.

             android:enabled="false" is deliberate and comes from AppCompat's own docs: the
             service is never started, it exists purely to hold the meta-data. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Downloads the barcode scanner module at install time instead of on
             the first scan, so the friends screen's "scan a code" doesn't stall
             the first time someone taps it (Play Services fetches it on demand
             otherwise). Note there is deliberately NO android.permission.CAMERA:
             the code scanner runs the camera in the Play Services process, which
             is the reason it was chosen over CameraX. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <!-- adjustResize is what makes the WindowInsets.ime inset reported under edge-to-edge:
             the window no longer resizes itself around the system bars, so the screens' own
             safeDrawing padding (see AppScreenBackground) is the only thing keeping a focused
             text field above the keyboard. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CommanderCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
