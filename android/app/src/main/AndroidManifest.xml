<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".CommanderCompanionApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommanderCompanion"
        tools:targetApi="31">

        <!-- Lets AppCompat persist the per-app language chosen in Settings across restarts
             (LocaleManager on API 33+, its own SharedPreferences-backed store below that). -->
        <meta-data
            android:name="autoStoreLocales"
            android:value="true" />

        <!-- Downloads the barcode scanner module at install time instead of on
             the first scan, so the friends screen's "scan a code" doesn't stall
             the first time someone taps it (Play Services fetches it on demand
             otherwise). Note there is deliberately NO android.permission.CAMERA:
             the code scanner runs the camera in the Play Services process, which
             is the reason it was chosen over CameraX. -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <!-- adjustResize is what makes the WindowInsets.ime inset reported under edge-to-edge:
             the window no longer resizes itself around the system bars, so the screens' own
             safeDrawing padding (see AppScreenBackground) is the only thing keeping a focused
             text field above the keyboard. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.CommanderCompanion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
