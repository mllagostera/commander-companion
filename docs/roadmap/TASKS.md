# Commander Companion — Lista de tareas

Checklist operativa de todo el trabajo pendiente, organizada por las **Etapas** definidas en [ROADMAP.md](ROADMAP.md). Este documento es la fuente de verdad del progreso día a día: el ROADMAP explica el *qué* y el *porqué*, este archivo trackea el *estado*.

**Cómo mantenerlo actualizado:**
- Marca `[x]` cuando una tarea esté terminada y funcionalmente completa (no solo "compila").
- Si algo está a medias (scaffolding sin lógica real, stub, dummy data), déjalo en `[ ]` y añade una nota entre paréntesis explicando qué falta.
- Añade tareas nuevas según aparezcan; no borres las completadas, son historial útil.
- Actualiza la fecha de "Última revisión" cada vez que se audite el estado real del código.

**Última revisión:** 2026-07-30 (sincronización de `main` tras mergear 3 PRs — #48, #49, #50 — y auditoría de los 3 cambios reales contra `TASKS.md`, que no mencionaba ninguno. (1) **Backend, PR #48** (`internal/common/migrate.go` nuevo): el binario `api` ahora aplica las migraciones de goose embebidas al arrancar (`common.RunMigrations`, conexión `database/sql` vía el driver stdlib de pgx, separada del `pgxpool` de la app, cerrada antes de abrir el pool real), corriendo antes de levantar Fiber. Motivo documentado en el propio commit: el free tier de Render no ofrece un hook de "pre-deploy/release command", así que las migraciones nunca llegaban a aplicarse en producción contra Supabase; con esto funciona igual en cualquier plataforma. `backend/Dockerfile` copia `migrations/` a la imagen final para que el binario tenga de dónde leerlas en runtime. `backend/README.md` gana una sección "Despliegue (Render)" con las env vars mínimas (`DB_URL` — Session pooler de Supabase, no Transaction pooler, porque el backend usa prepared statements vía pgx —, `JWT_SECRET`, `GOOGLE_CLIENT_ID`, `CORS_ALLOWED_ORIGINS`, `WEB_APP_URL`) y una nota de límite explícito: con más de una réplica, todas correrían `goose up` en paralelo al bootear — goose es idempotente pero no serializa esas ejecuciones concurrentes con un lock; inofensivo hoy porque el despliegue es de una sola instancia. Esto es preparación real para desplegar (Render + Supabase + Vercel, la Opción 1 que ya proponía el ROADMAP), pero **todavía no hay ADR formalizando la decisión de infraestructura ni IaC/workflow de deploy** (no existe `render.yaml` ni un job de CI que despliegue) — el ítem Transversal "Definir stack de infraestructura de despliegue" sigue abierto, ver nota agregada ahí. Nueva dependencia `github.com/pressly/goose/v3` (antes solo se usaba vía CLI/Makefile). (2) **Web, PR #49**: internacionalización completa con `@nuxtjs/i18n` — ver [ADR-0014](../decisions/0014-internacionalizacion-web.md), que ya documenta la decisión a fondo (un solo locale `es` activo, `strategy: 'no_prefix'`, ~200-250 claves en `web/i18n/locales/es.json`, namespacing por página/composable, pluralización/interpolación nativas de vue-i18n reemplazando ternarios manuales, y de paso corrige el copy de voseo argentino a tuteo de España). TASKS.md no tenía ninguna entrada para esto, agregada en Stage 4b más abajo. Mismo PR, dos cambios menores adicionales: botón de Google Sign-In en `login.vue` pasa de tema `outline` fijo (blanco, 320px) a forma `pill`, ancho del contenedor y tema dependiente del tema de la app (`filled_black` en oscuro); y Nuxt gana un `<title>` por defecto + `titleTemplate` (antes no seteaba ninguno, violación WCAG 2.4.2 marcada por axe/Lighthouse). (3) **Android, PR #50**: rediseño visual completo (Login, Dashboard, Historial, Setup, PreGame, GameTracker) siguiendo un mockup HTML de referencia — paleta violeta/oscura nueva (`Gradients.kt` nuevo: `AccentGradient`/`TitleGradient`/`ScreenBackgroundGradient`), componentes compartidos nuevos en `AppComponents.kt` (`GradientButton`, `GlassCard`, `PillSegmentedControl`, `SelectableChip`/`SelectableCircle`, `AuthTextField`, `RotateDevicePrompt`, etc.) que reemplazan el `PlayerCard.kt` que existía antes (su lógica de tarjeta de jugador ahora vive como `PlayerQuadrant` dentro de `GameTrackerScreen.kt`, ver `android/README.md` actualizado). `PreGameScreen`/`GameTrackerScreen` pasan a exigir orientación horizontal (`RotateDevicePrompt` si el dispositivo está en vertical) con una grilla de asientos (`QuadrantGrid`), botón/overlay de pausa (`PauseButton`/`PauseOverlay`) y resumen final de partida (`GameSummary`) nuevos — no existían antes. **Cambio funcional, no solo visual**: el contador de veneno pasa de no estar modelado (`GameState.kt` ya lo marcaba como fuera de alcance) a estarlo de verdad — `PlayerState.poison`, `PlayerState.isEliminated()` centraliza las 3 condiciones de eliminación (vida ≤0, veneno ≥10, daño de comandante ≥21, antes solo las dos primeras estaban en un `isAlive` privado de `GameViewModel`), `GameViewModel.adjustPoison` + `GameRepository.recordPoisonChange` espejan el cambio al backend igual que `LifeChange`/`CommanderDamage` (mismo patrón best-effort del asiento local, Stage 5). De paso se eliminó `GameViewModel.previousTurn()` (ya no se puede retroceder el contador de turno). Se agrega el **flujo de registro que no existía en Android** (`RegisterScreen`/`RegisterViewModel` nuevos, `RegisterRoute` en la navegación, accesible desde `LoginScreen`): llama a `POST /auth/register` (`AuthApi.register`, nuevo — antes esa interfaz solo tenía login/google/refresh/logout/me) con el mismo contrato que ya usa el cliente web — el registro no deja sesión iniciada (el backend no devuelve tokens, solo el `User` creado), así que la pantalla muestra "revisá tu email" en vez de navegar al dashboard; valida longitud mínima de password (8) client-side y mapea 409 (usuario/email duplicado) y 400 antes de reintentar. Detalle completo en Stage 4/5 más abajo. Revisión previa, 2026-07-28: pedido explícito del usuario: "revisá que no nos hayamos dejado documentación por actualizar". Encontrados 3 gaps reales, todos de código ya implementado y committeado en un cambio previo del mismo día que no había terminado de reflejarse en `TASKS.md`: (1) Stage 4b tenía `- [ ] Pantallas de playgroups en el cliente web` cuando `app/pages/playgroups/{index,[id]}.vue` + `usePlaygroups.ts`/`useGames.ts` ya existían completos (listado con conteo de miembros/partidas, detalle con renombrar/stats por miembro/agregar miembro/historial) — la entrada de Playgroups del Stage 1 ya documentaba el backend de esto, pero el checklist del cliente web nunca se marcó; (2) la pantalla de Ajustes (`app/pages/settings.vue`, `useSettings.ts`: cambio de password, vínculo de usuario de Moxfield, disparo + polling del import masivo) no tenía ninguna entrada — cambio de password es un endpoint backend nuevo (`POST /users/{id}/password`) que tampoco estaba en el Stage 1 "Auth"; (3) el bullet de `JoinGame` en Games/game-actions seguía afirmando "ya no acepta `user_id` en el body", una descripción que el proxy-join de ADR-0013 (documentado más abajo, en Playgroups) dejó desactualizada — corregido para explicar los dos modos. De paso, `GET /games?playgroup_id=` (`ListGamesForPlaygroup`) tampoco tenía bullet propio. `docs/api/openapi.yaml` y `docs/decisions/0013-*.md` ya estaban al día — el gap era puntual de `TASKS.md`. Revisión previa, mismo día: auditoría completa de `openapi.yaml` contra las rutas reales registradas en cada `handler.go` — pedido explícito del usuario. Se enumeraron las ~39 rutas de los 10 módulos y se compararon 1 a 1 contra los paths documentados. 3 discrepancias reales encontradas y corregidas: (1) `GET /games/{id}` (`GetGame`) es una ruta real que no tenía path propio en el spec — agregado; (2) `GET /users/{id}` estaba documentado pero no existe ningún handler que lo registre (`users.RegisterProtectedRoutes` solo tiene PATCH y POST /password) — eliminado del spec; (3) `PATCH /decks/{id}` estaba documentado pero tampoco existe (`decks.RegisterRoutes` solo tiene GET/POST/DELETE, no hay `UpdateDeck` en el `Service`) — eliminado. El resto de los ~36 paths y sus schemas (requests/responses) se verificaron campo por campo contra los DTOs Go correspondientes (`auth`, `sync`, `moxfieldimport`, `statistics`) sin encontrar más divergencias. Spectral sigue en 0 errores (101 warnings, todos preexistentes: operationId/tags faltantes, estilo no forzado por `.spectral.yaml`). Revisión previa, mismo día: `GET /users/search?q=` nuevo: buscar usuarios por username parcial o email exacto, para invitar a un playgroup sin conocer su UUID — cierra una nota literal de TODO que ya tenía el form de "agregar miembro" del web. Nunca expone el email de un tercero (`UserSearchResult` sin ese campo) ni al propio requester en el resultado. De paso se corrigió un bug real: sin resultados devolvía JSON `null` en vez de `[]` (slice sin inicializar), lo que rompe clientes JS/TS que asumen array. Web (`useUsers.ts`, `[id].vue`) reemplaza el input de UUID crudo por autocomplete con debounce. Ver la entrada de Playgroups más abajo. Revisión previa, mismo día: Android — `PlayerSetupScreen` gana un selector Casual/Grupo — Casual sigue siendo el life tracker de siempre garantizado sin red; Grupo deja elegir un playgroup real y asignar sus miembros a los asientos (con su deck), sentándolos a todos vía proxy-join en un solo bootstrap. `RemoteGameSession` pasa de un `localPlayerId` único a `seatPlayerIds: Map<Int, String>`, y `GameViewModel` espeja vida/daño de comandante de cualquier asiento presente en ese mapa — resuelve el TODO que impedía atribuir daño de comandante de un asiento sin `GamePlayer` propio. `PlayerConfig.isLocalUser` se eliminó (superado por `assignedUserId`/`assignedUsername`). Nuevo `PlaygroupRepository` + endpoints de playgroups en `CommanderApi`. Ver la entrada de Stage 5 más abajo para el detalle completo. Revisión previa, mismo día: backend — nueva capacidad de proxy-join (`POST /games/{id}/join` con `user_id`) autorizada por membresía de playgroup compartida, `game_players.added_by` nuevo (migración `00012`), y `POST /games/{id}/actions` ahora exige que el caller sea dueño del `GamePlayer` actor o quien lo proxy-joineó — cerrando de paso un hueco de autorización preexistente donde ese endpoint no validaba nada. `ListPlaygroupMembers` trae `username`; endpoint nuevo `GET /playgroups/{id}/members/{userId}/decks`. Ver [ADR-0013](../decisions/0013-proxy-join-y-autorizacion-de-acciones.md) y la entrada de Playgroups más abajo para el detalle. Revisión previa, mismo día: Stage 5, reemplazado el hardcode de "asiento 1 = usuario autenticado" + "primer deck del usuario" (`DeckRepository.firstDeckId`, ahora eliminado) por selección explícita en `PlayerSetupScreen` — chip "Soy yo" por asiento + picker de deck; `PlayerConfig` lleva `isLocalUser`/`deckId`, `GameRepository.bootstrapRemoteGame` recibe el `deckId` en vez de adivinarlo, `GameViewModel` calcula el asiento local desde la config. Reportado por el usuario al jugar una partida de prueba contra el stack local: las estadísticas no quedaban atribuidas a su cuenta porque no había forma de elegir asiento ni deck. `docs/ux/casos-de-uso.md` actualizado, gap cerrado. Revisión previa, mismo día: `android/gradle.properties` (committeado, valores por defecto del build) tenía `API_BASE_URL`/`GOOGLE_WEB_CLIENT_ID` con comillas simples literales alrededor del valor — sintaxis inválida en un `.properties`, quedaban pegadas al string y rompían el parseo de URL de OkHttp en runtime (`IllegalArgumentException` al arrancar la app); además `API_BASE_URL` apuntaba a `localhost:8000` en vez de `10.0.2.2:8080` (alias del emulador hacia el host + puerto real de `docker-compose.yml`) — corregido. Revisión previa, 2026-07-27: limpieza de tareas Transversales de infraestructura: (1) `docker/`/`scripts/` — confirmado que no existen ni tienen historial en `git log --all`, corregido el diagrama de árbol de `README.md` que las listaba; (2) `.github/dependabot.yml` nuevo, 4 ecosistemas (`gomod`/`backend`, `npm`/`web`, `gradle`/`android`, `github-actions`/raíz), semanal; (3) auditados los 4 workflows de CI job por job y corregido un gap real ya sufrido en la práctica: `backend-ci.yml`/`android-ci.yml`/`web-ci.yml` disparaban toda su batería pesada (para backend, incluye Postgres real) ante un cambio de *solo* el README del directorio, porque el filtro `backend/**`/`android/**`/`web/**` de `dorny/paths-filter` también matchea esos README — arreglado con `predicate-quantifier: 'every'` + patrones de exclusión `!*/README.md`/`!*/**/*.md`, separando el patrón del propio workflow file en un filtro aparte (`*_workflow`, combinado con `||` en cada `if`) ya que bajo `'every'` esas dos rutas no pueden convivir en la misma lista de patrones. Revisión previa, mismo día: auditoría completa de documentación contra el estado real del código, con foco en centralizar el hub en el `README.md` de la raíz — sección 8 reescrita como índice completo de *todo* documento del repo, antes solo enlazaba 3. Hallazgo principal, vía exploración dedicada de Android: los docs de UX/navegación (`docs/ux/casos-de-uso.md`, `docs/ux/wireframes.md`, `docs/diagrams/android-navigation-flow.md`) se habían escrito en paralelo, en otro worktree, al mismo commit que conectó Android al backend de verdad (`CommanderApi.kt` con 15 endpoints reales, `GameRepository`/`DeckRepository`, espejo best-effort de partidas, tests unitarios nuevos) — quedaron describiendo el Android *anterior* a ese commit. Los tres se corrigieron a fondo (login ya autentica, el tracker ya no es 100% local, `RemoteSyncBanner`/botón de logout documentados). También correcciones puntuales: `README.md` decía "no existe `web-ci.yml`" cuando sí existe (agregada su descripción + nota sobre qué checks son requeridos hoy en branch protection y cuáles no); `TASKS.md` mismo (Stage 4/5/Transversal) tenía ítems `[ ]` que ya estaban hechos (`CommanderApi.kt`, repositorios, tests de Android — pasaban de "0 tests" a 5 archivos reales); ADR-0004 (cliente web) seguía diciendo "pendiente" sobre Moxfield/estadísticas ya completos; ADR-0009 decía "Retrofit para consumo futuro" y "sin capa de repositorio" ya resueltos; ADR-0010 decía `internal/websocket/` vacío cuando ADR-0005 ya documenta su implementación; `docs/diagrams/er-diagram.md` no tenía la columna `decks.image_url`; `ARCHITECTURE.md`/`ROADMAP.md` no mencionaban el cliente Web en ningún lado (agregada sección/stage). Revisión previa, mismo día: PostgreSQL actualizado a la última versión mayor estable, 18 (`postgres:18-alpine`), en `docker-compose.yml` y `backend-ci.yml` — antes desalineados en `15-alpine`/`16-alpine` respectivamente; ajustado también el mount del volumen de datos al nuevo layout que exigen las imágenes 18+ (`/var/lib/postgresql` en vez de `/var/lib/postgresql/data`); volumen de dev recreado y migraciones re-aplicadas; verificado con la suite de tests de integración completa contra Postgres 18.4 real. Además, deck importado de Moxfield ahora guarda y muestra el art crop del comandante (`decks.image_url`, migración `00006_deck_image_url.sql`, tomado del campo `main.id` de la respuesta de Moxfield — el mismo que usa su propio `og:image`). Revisión previa, mismo día: repaso de consistencia de documentación tras la ronda anterior: `docker-compose.yml` centralizado en la raíz + `web/Dockerfile` nuevo, con dos bugs reales corregidos al probar el stack completo (healthcheck de `db`, split `NUXT_API_BASE`/`NUXT_PUBLIC_API_BASE` para SSR-en-Docker) — verificado end-to-end (registro → login → render SSR autenticado); ADR-0004 actualizado con esas consecuencias; creado `backend/README.md` (no existía pese a estar referenciado desde `web/README.md` y `tools/auth-test/README.md`); corregida referencia rota a `backend/docker-compose.yml` en `tools/auth-test/README.md`; limpieza de artefactos residuales de Windows (`backend/migrations;C/`, `dbml-error.log` vacío en la raíz). Revisión previa, mismo día: cierre de los 4 entregables pendientes de Stage 0: casos de uso, wireframes, diagramas de flujo/estado y ADRs fundacionales — ver Stage 0 más abajo. + Android conectado al backend real de auth: `LoginScreen`/`LoginViewModel` autentican de verdad contra `POST /auth/login`/`POST /auth/google` (Credential Manager + Google Identity Services), `NetworkModule` (Retrofit/OkHttp) nuevo, `SessionManager` persiste la sesión en DataStore, interceptor+authenticator de refresh-on-401, y logout real con `clearCredentialState` — pendiente solo el paso manual externo de crear el Web Client ID de Google Cloud, código ya listo para recibirlo + cierre de Stage 2 "base de datos": índices explícitos (`00003_indices.sql`), `CHECK` constraints de `games.status`/`game_actions.action_type` (`00004_status_constraints.sql`) y diagrama ER exportado (`docs/diagrams/er-diagram.md`), todo probado up/down/up contra Postgres real + esqueleto inicial del cliente web Nuxt con login email/password + Google + Stage 6: diseño ([ADR-0005](../decisions/0005-websocket-protocol.md)) e implementación real del servidor de sincronización en vivo por WebSocket, `internal/websocket/`, wireado a `games`/`game-actions` sin dependencia dura. Revisión anterior, 2026-07-26: auditoría inicial + detalle de auth con Google OAuth + generación de slices playgroups/games/game-actions y fix de tooling sqlc/lint + quality gates de GitHub Actions + repo vinculado y branch protection activo en `main` + implementación real de auth email/password + Google + CORS + herramienta de test manual `tools/auth-test/` + wiring real de `decks` e import de Moxfield + primeros tests de integración de backend en `auth`/`decks`/`moxfield` + wiring real de `games`/`game-actions`, el motor de partida + wiring real de `playgroups` y `statistics` (recálculo al finalizar partida) — **backend 100% conectado a la base de datos real**, sin módulos dummy pendientes + life tracker local de Android completo con persistencia en Room + `PreGameScreen` con randomizador de turno y tracking de mulligans)

---

## Stage 0: Definición funcional

- [x] Roadmap general (`docs/roadmap/ROADMAP.md`)
- [x] Documento de arquitectura y principios (`docs/architecture/ARCHITECTURE.md`)
- [x] Casos de uso detallados (flujos de usuario paso a paso: crear partida, unirse, trackear vida, finalizar, ver stats) — [`docs/ux/casos-de-uso.md`](../ux/casos-de-uso.md), basado en el código real de `games`/`game-actions` (backend) y de las pantallas de setup/pregame/tracker/history (Android), documentando explícitamente dónde diverge "hoy" (Android 100% local) de "objetivo" (flujo contra el backend)
- [x] Wireframes de las pantallas Android (`docs/ux/` está vacío) — [`docs/ux/wireframes.md`](../ux/wireframes.md): wireframes ASCII + jerarquía de componentes de `LoginScreen`, `DashboardScreen`, `PlayerSetupScreen`, `PreGameScreen`, `GameTrackerScreen` y `HistoryScreen`, leídos del Compose real de cada pantalla
- [x] Diagramas adicionales de flujo/estado (`docs/diagrams/` está vacío; solo existen los 2 diagramas de arquitectura embebidos en el ROADMAP) — [`docs/diagrams/game-state-machine.md`](../diagrams/game-state-machine.md) (máquina de estados `pending → active → finished` con los guards exactos de `games/service.go`, más el sub-estado de eliminación de jugador de `game-actions/service.go`) y [`docs/diagrams/android-navigation-flow.md`](../diagrams/android-navigation-flow.md) (grafo real de `AppNavigation.kt`/`Routes.kt`)
- [x] ADRs de las decisiones fundacionales del proyecto (anteriores a esta sesión, sin contexto propio para redactarlas honestamente): elección de Go+Fiber, PostgreSQL, sqlc+goose, Android nativo (Kotlin/Compose) vs. cross-platform, monolito modular vs. microservicios — redactadas retroactivamente como "decisión heredada, contexto reconstruido" (2026-07-27), numeradas 0006-0010 para evitar colisión con un `0005-websocket-protocol.md` en curso en paralelo en otro worktree:
  - [ADR-0006](../decisions/0006-go-fiber-backend.md): Go + Fiber para el backend
  - [ADR-0007](../decisions/0007-postgresql.md): PostgreSQL como base de datos principal
  - [ADR-0008](../decisions/0008-sqlc-goose.md): sqlc para acceso a datos tipado + goose para migraciones
  - [ADR-0009](../decisions/0009-android-nativo-vs-crossplatform.md): Android nativo (Kotlin + Compose) vs. cross-platform
  - [ADR-0010](../decisions/0010-monolito-modular-vs-microservicios.md): monolito modular vs. microservicios
- [x] ADRs de las decisiones técnicas tomadas en esta sesión, en `docs/decisions/`:
  - [ADR-0001](../decisions/0001-auth-jwt-refresh-token-strategy.md): estrategia de auth (JWT HS256 + refresh token opaco rotativo)
  - [ADR-0002](../decisions/0002-google-sign-in.md): Google Sign-In (auto-vinculación por email verificado, librería `go-oidc`)
  - [ADR-0003](../decisions/0003-cors-permisivo-en-dev.md): CORS abierto por defecto en dev
  - [ADR-0004](../decisions/0004-web-client-nuxt.md): segundo cliente web con Nuxt 4 (esqueleto de auth iniciado)
  - [ADR-0005](../decisions/0005-websocket-protocol.md): protocolo de sincronización en vivo por WebSocket (servidor implementado; ver Stage 6)

## Stage 1: Backend (base del proyecto)

- [x] Proyecto Go inicializado (`go.mod`, `cmd/api/main.go`, Fiber)
- [x] Estructura modular en `internal/` (auth, users, decks, games, game-actions, playgroups, statistics, sync, websocket, common)
- [x] Slices `playgroups`, `games` y `game-actions` generados (query.sql + dto/service/handler esqueleto, CRUD y flujo create→join→leave→start→finish→actions→timeline) y registrados en `cmd/api/main.go`. Los tres módulos ya tienen lógica real (ver Stage 1 y nota de wiring más abajo).
- [x] `sqlc generate` corriendo para los 6 módulos con queries (`users`, `decks`, `playgroups`, `games`, `game-actions`, `statistics`) — se corrigió `sqlc.yaml`, que no tenía `sql_package: "pgx/v5"` y generaba un `DBTX` incompatible con `pgxpool.Pool` (no compilaba)
- [x] **Módulo `auth` real** — `internal/auth/` implementado: JWT (access token) + refresh token opaco con rotación, middleware Fiber, login/refresh/logout/me/google

### Auth — email/password
- [x] Hash de contraseñas con bcrypt (`internal/users/service.go: RegisterUser`, `bcrypt.GenerateFromPassword`/`CompareHashAndPassword`)
- [x] `POST /auth/login` real: verifica credenciales contra la BD (`users.VerifyCredentials`) y emite access token (JWT HS256) + refresh token
- [x] `POST /auth/refresh` real: valida el refresh token contra `refresh_tokens` (hash SHA-256 persistido, el token en claro nunca se guarda), lo **revoca y rota** (nuevo refresh token en cada uso) y emite un nuevo access token
- [x] `POST /auth/logout` real: revoca el refresh token indicado (`revoked_at`) contra la tabla `refresh_tokens`
- [x] `GET /auth/me` real: resuelve el usuario desde el `user_id` que dejó el middleware en el contexto (`common.UserIDKey`)
- [x] Middleware Fiber (`auth.RequireAuth`) que valida el Bearer JWT y mete el `user_id` en `c.Locals(common.UserIDKey)`
- [x] Middleware aplicado a todas las rutas protegidas vía grupo `api.Group("", auth.RequireAuth(...))` en `main.go`; eliminados los `"dummy-user-id"` hardcodeados de `decks/handler.go` y `statistics/handler.go` (ahora leen `common.UserIDKey`)
- [x] Expiración y firma de tokens: **HS256 simétrico** con `JWT_SECRET` (env var, default inseguro solo de dev con `//nolint:gosec` justificado) — decisión: RS256 no aporta valor en un monolito sin verificadores externos del token; TTLs configurables por env (`ACCESS_TOKEN_TTL` default 15m, `REFRESH_TOKEN_TTL` default 720h/30d). Detalle y alternativas consideradas: [ADR-0001](../decisions/0001-auth-jwt-refresh-token-strategy.md)

### Auth — Google OAuth (Sign-In)
- [x] ADR: decisión de añadir Google como proveedor OAuth junto a email/password (alcance: solo login social, no reemplaza al password) — [ADR-0002](../decisions/0002-google-sign-in.md)
- [ ] **Crear credenciales OAuth en Google Cloud Console** (Web Client ID + Android Client ID) — paso manual externo, requiere acceso a una cuenta de Google Cloud del proyecto; el backend ya está listo para recibirlo vía `GOOGLE_CLIENT_ID`. Sin configurar, `POST /auth/google` responde `501 Not Implemented` en vez de crashear
- [x] Cambios de esquema en BD (ver Stage 2): `google_id` en `users`, `password_hash` nullable, `CHECK (password_hash IS NOT NULL OR google_id IS NOT NULL)` (migración `00002_auth.sql`, probada up/down/up contra Postgres real)
- [x] `POST /auth/google`: recibe `id_token`, lo verifica contra Google (issuer + audience = `GOOGLE_CLIENT_ID` + firma vía JWKS) y emite el mismo par access/refresh token que `/auth/login`
- [x] Verificación del `id_token` en Go — se usó `github.com/coreos/go-oidc/v3` en vez de `google.golang.org/api/idtoken`: mismo resultado (discovery + JWKS + validación de issuer/audience/firma) con una huella de dependencias mucho más liviana (evita arrastrar todo `google.golang.org/api` + gRPC + OpenTelemetry solo para verificar un token). El discovery document de Google se resuelve de forma perezosa en el primer login con Google, no al arrancar el servidor
- [x] Lógica de alta/vinculación de cuenta (`users.FindOrCreateGoogleUser`): busca por `google_id` → si no existe, busca por email y **vincula automáticamente** si Google confirma `email_verified` → si tampoco existe, crea usuario nuevo sin password (username derivado del local-part del email, con reintento sufijado ante colisión)
- [x] Variable de entorno `GOOGLE_CLIENT_ID` documentada en `.env.example`
- [x] Manejo de errores específicos: token inválido/expirado → 401, `email_verified: false` → 400, `GOOGLE_CLIENT_ID` no configurado → 501

### Auth — verificación de email
- [x] ADR: decisión de bloquear login hasta confirmar el email, y de usar Resend con templates de dashboard (no HTML en el backend) — [ADR-0012](../decisions/0012-verificacion-email-resend.md)
- [x] Migración `00011_email_verification.sql`: `users.email_verified` (default `true`; solo `CreateUser` fuerza `false`), tabla `email_verification_tokens` (mismo patrón hash+TTL que `refresh_tokens`)
- [x] `users.RegisterUser` genera el token, lo persiste hasheado (TTL 24h) y dispara el mail vía `users.Mailer` (interfaz angosta, mismo patrón que `decks.MoxfieldClient`); un fallo de envío no revierte el registro
- [x] `users.VerifyCredentials` (de la que depende `auth.Login`) devuelve `403 ErrEmailNotConfirmed` si el password es correcto pero el email no está confirmado
- [x] `POST /auth/verify-email` y `POST /auth/resend-verification` (`internal/users/handler.go`), ambos bajo el mismo rate limit por IP que el resto de los endpoints públicos de auth; `ResendVerification` nunca revela si el email existe o ya está verificado
- [x] `LinkGoogleID` marca `email_verified = true` al vincular una cuenta de Google a una cuenta email/password existente (Google ya confirmó ese email en `FindOrCreateGoogleUser` antes de llegar a este punto)
- [x] `internal/email`: cliente de Resend (`template: { id, variables }`, sin HTML/texto embebido en Go) + mailer "de consola" cuando `RESEND_API_KEY` está vacío, para que desarrollo local y tests no dependan de una cuenta de Resend
- [x] Frontend web: `register.vue` ya no auto-loguea (muestra "revisá tu email"), `login.vue` ofrece reenviar verificación ante un `403`, página nueva `verify-email.vue`
- [x] Flag `REQUIRE_EMAIL_VERIFICATION` (`config.Config.RequireEmailVerification`, default `false`): en `false`, fase alpha actual, `RegisterUser` crea la cuenta ya verificada y no genera token ni llama al `Mailer` — no se gasta el envío mientras nadie va a exigir el click. Todo lo de arriba queda construido pero inactivo hasta prender el flag
- [ ] **Salir de alpha: prender `REQUIRE_EMAIL_VERIFICATION=true`**, y antes de eso, **verificar un dominio propio en el dashboard de Resend** (SPF/DKIM/DMARC) y crear ahí el Template de verificación (variables `USERNAME`/`VERIFY_URL`, publicado, `VERIFY_URL` como texto plano por el bug de Resend documentado en el ADR) — paso manual externo, requiere acceso al dominio y a la cuenta de Resend del proyecto; sin `RESEND_API_KEY`/`RESEND_VERIFY_EMAIL_TEMPLATE_ID` configurados, el link de verificación se loguea por consola en vez de mandarse

### Games / game-actions — motor de partida
- [x] `games` conectado a `Queries` reales (sqlc): `CreateGame`, `GetGame`/`ListGames` (con `players` embebido en el detalle), `JoinGame`, `LeaveGame`, `StartGame`, `FinishGame`
- [x] Máquina de estados `pending → active → finished` aplicada server-side: `join`/`leave` solo en `pending`, `start` requiere ≥2 jugadores (`minPlayersToStart`) y solo desde `pending`, `finish` solo desde `active`, cualquier transición inválida devuelve 409
- [x] `JoinGame`: sin `user_id` en el body, el jugador es siempre el usuario autenticado vía JWT (comportamiento original, cerraba el hueco de que cualquiera anotara jugadores a nombre de otro usuario). **Actualizado 2026-07-28**: `user_id` opcional reintroducido a propósito, pero ya no de forma abierta — solo autorizado como proxy-join si caller y destino comparten el `playgroup_id` de la partida (ver [ADR-0013](../decisions/0013-proxy-join-y-autorizacion-de-acciones.md) y la entrada de Playgroups más abajo). Se verifica ownership del deck contra el usuario destino, no el caller (mismo criterio que `decks`: 404 "deck not found" tanto si no existe como si es de otro usuario) y que el usuario destino no esté ya sentado en esa partida (409)
- [x] `GET /games?playgroup_id=` (2026-07-28, `ListGamesForPlaygroup`): historial completo (sin paginar) de partidas de un grupo, requiere que el caller sea miembro (404 si no, mismo criterio de no-revelar). Consumido por `web/app/composables/useGames.ts` en la pantalla de detalle de un playgroup
- [x] `game-actions` conectado a `Queries` reales: valida `action_type` contra el vocabulario fijo (`LifeChange`, `CombatDamage`, `CommanderDamage`, `PoisonCounter`, `TurnStart`, `TurnEnd`, `Elimination`), resuelve actor/target como `game_players` de esa partida (no `users`) y solo permite registrar acciones si la partida está `active`
- [x] Los actions mutan el estado real del jugador afectado (no solo quedan en el log): `LifeChange`/`CombatDamage`/`CommanderDamage` ajustan `life_total`, `PoisonCounter` ajusta `poison_counters`, `Elimination` marca `is_eliminated`; auto-eliminación server-side cuando `life_total <= 0` o `poison_counters >= 10` (reglas estándar de Commander)
- [x] Tests de integración (`internal/games/service_test.go`, `internal/game-actions/service_test.go`, Postgres real) cubriendo la máquina de estados completa y los efectos de cada `action_type`, además del smoke test manual end-to-end por curl (ver Transversal)
- [x] `CommanderDamage` distingue la fuente del daño por oponente (2026-07-27) — tabla nueva `commander_damage` (`migrations/00007_commander_damage.sql`, PK compuesta `(attacker_id, defender_id)`) trackea el acumulado por par atacante-defensor con un upsert atómico (`ON CONFLICT ... amount = amount + EXCLUDED.amount`); `applyCommanderDamage` (`internal/game-actions/service.go`) elimina al defensor si ese acumulado llega a 21, independiente de `life_total` (puede quedar con vida positiva). Exige `target_id` distinto del actor (400 si no). De paso, **primera transacción del proyecto**: todo `RecordAction` (resolución de actor/target, mutación de estado, log de la acción) ahora corre dentro de un único `pgx.Tx`, cerrando un bug latente preexistente en los otros 6 tipos de acción (antes, un crash a mitad de camino podía dejar un cambio de vida aplicado sin su entrada de log correspondiente)
- [x] `TurnStart`/`TurnEnd` ya no son solo marcadores de log (2026-07-27) — `games.current_turn_player_id` (nullable, `migrations/00008_current_turn.sql`): `TurnStart` lo fija al actor, `TurnEnd` lo limpia (`internal/game-actions/service.go: setCurrentTurn`/`clearCurrentTurn`, dentro de la misma transacción de `RecordAction`). Deliberadamente **no** modela orden de turno (quién sigue, `seat_number`) — solo responde "de quién es el turno ahora", que es lo único que pedía este ítem. Expuesto en `GameResponse.current_turn_player_id` (`internal/games`) y en `openapi.yaml`
- [x] `openapi.yaml` actualizado: `CreateGameRequest`, `JoinGameRequest`, `Game.players`/`Game.finished_at`, enum de `action_type` y responses 400/404/409 en los endpoints de `/games/*`

### Playgroups — grupos de juego
- [x] `playgroups` conectado a `Queries` reales: `CreatePlaygroup` auto-une al creador como primer miembro; `GetPlaygroup`/`ListPlaygroups` quedan **acotados a membresía** (un usuario solo ve/lista los grupos de los que es miembro; no hay listado público de todos los grupos de la plataforma)
- [x] `GetPlaygroup` de un grupo del que no sos miembro devuelve 404 sin distinguir de "no existe" (mismo criterio que `decks`/`games`), para no revelar grupos ajenos por ID
- [x] `AddMember` requiere que quien invita ya sea miembro del grupo (404 si no lo es, mismo criterio de no-revelar) y que el usuario a añadir exista (404 si no) y no esté ya en el grupo (409)
- [x] Tests de integración (`internal/playgroups/service_test.go`, Postgres real): auto-join del creador, scoping por membresía, y las 3 validaciones de `AddMember`
- [x] **Proxy-join y autorización de acciones por `game_players.added_by`** (2026-07-28, ver [ADR-0013](../decisions/0013-proxy-join-y-autorizacion-de-acciones.md)): un usuario autenticado puede unir a OTRO usuario a una partida (`POST /games/{id}/join` con `user_id` en el body) si ambos comparten el `playgroup_id` de la partida — habilita "un solo dispositivo anota por toda la mesa" para el modo Grupo de Android, con estadísticas reales para cada jugador, no solo para quien sostiene el teléfono. `ListPlaygroupMembers` ahora trae `username` (join a `users`) y hay un endpoint nuevo, `GET /playgroups/{id}/members/{userId}/decks`, para que el "scorekeeper" pueda ver los decks de un compañero al armar el proxy-join. Al mismo tiempo se cerró un hueco de autorización preexistente e independiente de este pedido: `POST /games/{id}/actions` nunca validaba que `actor_id` perteneciera al usuario autenticado (el handler ni siquiera leía el JWT) — ahora exige que el caller sea el dueño del `GamePlayer` o quien lo proxy-joineó (`added_by`), si no, 403. Migración `00012_game_player_proxy_join.sql`. Tests: `internal/games/service_test.go` (proxy-join success/sin-playgroup/sin-membresía-caller/sin-membresía-target/deck-ajeno), `internal/game-actions/service_test.go` (dueño ok, proxy ok, ajeno 403).
- [x] **Búsqueda de usuarios por username/email** (2026-07-28): pedido explícito del usuario — el form de "agregar miembro" del web (`web/app/pages/playgroups/[id].vue`) tenía una nota literal, "Todavía no hay forma de buscar usuarios por nombre: pedile a tu amigo su ID de usuario". Nuevo `GET /users/search?q=` (`internal/users`): matchea por `username` (ILIKE, contiene, case-insensitive) y/o `email` (**exacto**, a propósito — parcial permitiría enumerar direcciones ajenas por prefijo/substring, ver comentario en `query.sql`). Nunca expone el email de un tercero en el resultado (`UserSearchResult{id, username}`, sin email, a diferencia de `UserResponse`), excluye al propio requester, cap de 10 resultados, mínimo 2 caracteres. **Bug real encontrado y corregido en el camino**: sin resultados, el slice `nil` serializaba a JSON `null` en vez de `[]` — rompe cualquier cliente JS/TS que asuma siempre-array (`.length`/`.filter()`); test de regresión agregado (`TestSearchUsers_NoResults_ReturnsEmptySliceNotNil`). Web: `useUsers.ts` nuevo + `[id].vue` reemplaza el input de UUID crudo por un autocomplete con debounce (300ms) que filtra a quienes ya son miembros. Verificado end-to-end contra el backend real (username parcial, email exacto, email parcial rechazado, exclusión de uno mismo, query corto → 400, `[]` no `null`).
- [x] **Renombrar grupo y listar decks de un miembro** (2026-07-28): `PATCH /playgroups/{id}` (`UpdatePlaygroup`) — solo un miembro existente puede renombrar, mismo criterio de no-revelar que el resto del módulo. `GET /playgroups/{id}/members/{userId}/decks` (`ListMemberDecks`) — decks de un compañero de grupo, autorizado si tanto el caller como el destino son miembros; es lo que permite elegir "con qué deck juega" al armar un proxy-join (ver ADR-0013). `ListPlaygroups` ahora también trae los `members` poblados de cada grupo (antes solo `GetPlaygroup` lo hacía) para que el listado del web muestre cantidad de miembros sin un round-trip extra por grupo.

### Statistics — recálculo real y consultas
- [x] `games/service.go: FinishGame` ya no tiene el TODO — dispara `statistics.RecalculateForGame(gameID)` de verdad tras finalizar (vía una interfaz `games.StatisticsRecalculator` para poder mockearla en tests; wiring real en `main.go` con `statistics.NewService` inyectado a `games.NewService`)
- [x] `RecalculateForGame` recorre a los jugadores y acciones de la partida ya finalizada y actualiza (upsert incremental, `ON CONFLICT DO UPDATE`) `user_statistics_summary` y `deck_statistics_summary` para cada participante:
  - `games_played`: +1 para todos los participantes
  - `games_won`: +1 solo si hay un único jugador no eliminado al finalizar (`sole survivor`); si la partida se cortó a mano con 2+ sobrevivientes, no se acredita victoria a nadie (pero sí se cuenta `games_played`)
  - `total_damage_dealt`/`total_commander_damage_dealt`: suma de los `amount` de `CombatDamage`/`CommanderDamage` atribuidos al actor
  - `highest_life_total_achieved` (por deck): recalculado **repitiendo el log de acciones** en orden cronológico desde el baseline de 40, no solo tomando el valor final de `life_total`
  - `total_eliminations`: cuenta únicamente acciones `Elimination` explícitas con un target distinto del actor — las auto-eliminaciones por vida/veneno (ver `game-actions`) no quedan atribuidas a un actor específico en el log, así que no suman acá (documentado como límite conocido, no como bug)
- [x] `GetUserStats`/`GetDeckStats` reales: leen de las tablas de resumen; si un usuario/deck nunca terminó una partida no hay fila (no es un error) y se devuelven ceros en vez de 404
- [x] `GetDeckStats` con ownership check (404 si el deck no es del usuario autenticado, mismo criterio que `decks`)
- [x] `GetPlaygroupStats` (antes `501 Not Implemented` hardcodeado) implementado con agregación **en vivo** (no hay tabla de resumen por grupo): partidas finalizadas del grupo + jugadas/ganadas por miembro, con el mismo criterio de "único sobreviviente" para determinar el ganador
- [x] Tests de integración (`internal/statistics/service_test.go`, Postgres real, arman partidas completas vía `games`+`game-actions` reales): ganador acreditado con daño correcto, sin ganador cuando hay 2+ sobrevivientes, acumulación entre partidas del mismo usuario, ownership de `GetDeckStats`, ceros por defecto, y agregación de `GetPlaygroupStats`
- [x] `openapi.yaml` actualizado: `CreatePlaygroupRequest`/`AddMemberRequest`/`Playgroup.members`, y `UserStatsResponse`/`DeckStatsResponse`/`PlaygroupStatsResponse` documentados (antes no existían en el spec)

### Infra / configuración
- [x] Centralizar la configuración leída de variables de entorno (2026-07-27) — `internal/config/config.go` nuevo: un struct `Config` (`DBURL`, `Port`, `CORSAllowedOrigins`, `Auth`) + una función `Load()` que reemplaza los 4 puntos sueltos de `os.Getenv` que tenía `cmd/api/main.go` (conexión a BD, puerto, CORS, y el ya existente `loadAuthConfig`), mismos defaults de siempre, sin cambiar comportamiento. **Decisión explícita**: no se crea una carpeta `configs/` con archivos por entorno (dev/staging/prod) todavía — el proyecto no usa ninguna librería de config por archivo (confirmado en `go.mod`) y el stack de despliegue aún no está decidido (ver ítem Transversal "Definir stack de infraestructura de despliegue"); anticipar un formato de archivo antes de esa decisión sería sobre-ingeniería. Si se decide un formato de archivo más adelante, `config.Load()` ya es el único punto que habría que tocar
- [x] `.env.example` documentando las variables requeridas (`DB_URL`, `PORT`, `JWT_SECRET`, `ACCESS_TOKEN_TTL`, `REFRESH_TOKEN_TTL`, `GOOGLE_CLIENT_ID`, `CORS_ALLOWED_ORIGINS`); `docker-compose.yml` actualizado con las mismas (y corregido: usaba `DB_HOST`/`DB_USER`/etc. sueltas que `main.go` nunca leyó — ahora usa `DB_URL` real)
- [x] CORS habilitado (`github.com/gofiber/fiber/v2/middleware/cors`), configurable vía `CORS_ALLOWED_ORIGINS` (vacío = `*`, razonable en dev porque la API solo usa Bearer tokens, nunca cookies). Necesario para que cualquier frontend web (en su momento `tools/auth-test/`, hoy el cliente Nuxt) pueda llamar a la API desde el navegador. Detalle: [ADR-0003](../decisions/0003-cors-permisivo-en-dev.md)
- [x] `tools/auth-test/`: página HTML de un solo archivo, sin build, para probar manualmente el flujo de auth completo (password + Google Sign-In real vía Google Identity Services) contra una instancia corriendo del backend. No era parte del producto; eliminada 2026-07-29 al quedar redundante una vez que Android y Web autentican de verdad contra el backend
- [x] `docker-compose.yml` centralizado en la raíz del repo (2026-07-27, antes vivía en `backend/`; `docker/` sigue vacío, no se usó como destino) con tres servicios — `db` (Postgres, ahora con `healthcheck: pg_isready`), `api` (`backend/Dockerfile`, ya existía) y `web` (`web/Dockerfile` nuevo, build Node 24 multi-stage que corre `node .output/server/index.mjs`). Corregidos dos bugs reales encontrados al levantar el stack completo: (1) `api` no esperaba a que Postgres estuviera *healthy* y crasheaba en frío — se agregó `depends_on: condition: service_healthy`; (2) las llamadas SSR del contenedor `web` (ej. `GET /auth/me` al cargar `/`) no pueden resolver `localhost:8080` desde dentro del propio contenedor — se separó `NUXT_API_BASE` (interno, `http://api:8080/api/v1`) de `NUXT_PUBLIC_API_BASE` (navegador, `http://localhost:8080/api/v1`) en `web/nuxt.config.ts`/`useAuth.ts`. Verificado end-to-end: `docker compose up --build` → migrar → registrar usuario → login → `/` renderiza autenticado vía la red interna de Compose. Sin lint/CI propio todavía (`web/Dockerfile` no tiene un `hadolint` equivalente al de `backend-ci.yml`, no existe `web-ci.yml`)
- [x] Limpieza: carpeta residual `backend;C` en la raíz del repo (eliminada, 2026-07-26)
- [x] PostgreSQL actualizado a la última versión mayor estable (**18**, `postgres:18-alpine`) en `docker-compose.yml` (antes `15-alpine`) y en el servicio `postgres` de `backend-ci.yml` (antes `16-alpine`) — las dos imágenes quedaron desalineadas entre sí antes de este cambio. Requirió ajustar además el mount del volumen `db` en `docker-compose.yml`: las imágenes 18+ de Postgres cambiaron su layout esperado de `/var/lib/postgresql/data` a un único mount en `/var/lib/postgresql` (el entrypoint arma un subdirectorio versionado adentro, pensado para `pg_upgrade --link`); sin el ajuste el contenedor no arranca (`Error: ... there appears to be PostgreSQL data in: /var/lib/postgresql/data (unused mount/volume)`). Salto de versión mayor incompatible con el volumen de datos previo — se recreó (`docker compose down -v`, dato de dev, no de producción) y se volvieron a aplicar las 6 migraciones de goose. Verificado end-to-end: stack completo levantado contra Postgres 18.4 real, suite de tests de integración del backend completa (`go test -race -cover -p 1 ./...`) pasando contra la base nueva, y smoke test manual de la API (registro/login)
- [x] **Migraciones de goose aplicadas automáticamente al arrancar el binario** (2026-07-29, `internal/common/migrate.go` nuevo, `common.RunMigrations`) — corren embebidas antes de levantar Fiber, contra una conexión `database/sql` separada del `pgxpool` de la app (driver stdlib de pgx), cerrada apenas terminan. Motivo: el free tier de Render no ofrece un hook de "pre-deploy/release command" para correr `goose up` antes de servir tráfico, así que las migraciones nunca llegarían a aplicarse en producción contra Supabase; con esto el propio binario deja el schema al día en cualquier plataforma sin depender de un hook específico. `backend/Dockerfile` copia `migrations/` a la imagen final (antes no lo hacía) para que el binario tenga de dónde leer los `.sql` en runtime. **Límite conocido y documentado**: con más de una réplica, todas correrían `goose up` en paralelo al bootear — goose es idempotente (`goose_db_version`) pero no serializa esas ejecuciones concurrentes con un lock; sin efecto hoy porque el despliegue sigue siendo de una sola instancia. Nueva dependencia `github.com/pressly/goose/v3` (antes solo se invocaba vía el `goose` CLI del `Makefile`)
- [x] **Groundwork de despliegue en Render documentado** (2026-07-29, `backend/README.md`, sección "Despliegue (Render)") — env vars mínimas para el servicio: `DB_URL` (connection string de Supabase, **Session pooler** y no Transaction pooler, porque el backend usa prepared statements vía pgx), `JWT_SECRET` (uno nuevo, no el default de dev), `GOOGLE_CLIENT_ID`, `CORS_ALLOWED_ORIGINS`, `WEB_APP_URL` (dominio del frontend en Vercel). Esto es la Opción 1 que ya proponía [ROADMAP.md](ROADMAP.md#infraestructura-de-despliegue-pendiente-de-decisión) (Render + Supabase + Vercel) empezando a materializarse en código/docs, pero **todavía no hay ADR formalizando la decisión** ni `render.yaml`/workflow de CI que despliegue — el ítem Transversal "Definir stack de infraestructura de despliegue" sigue abierto (ver nota ahí)

## Stage 2: Base de datos

- [x] Esquema DBML inicial (`docs/database/schema.dbml`)
- [x] Migración inicial coherente con el DBML (`migrations/00001_initial_schema.sql`)
- [x] Índices explícitos más allá de las PK: `decks.moxfield_id`, `game_actions.game_id`, `game_players.game_id` para lecturas frecuentes del timeline/estado (`migrations/00003_indices.sql`, `docs/database/schema.dbml` actualizado con los bloques `indexes {}` correspondientes). Probado up/down/up contra Postgres real (Docker); confirmados con `\d` que los índices quedan creados
- [x] Constraints adicionales: `CHECK (status IN ('pending', 'active', 'finished'))` en `games` y `CHECK (action_type IN ('LifeChange', 'CombatDamage', 'CommanderDamage', 'PoisonCounter', 'TurnStart', 'TurnEnd', 'Elimination'))` en `game_actions` (`migrations/00004_status_constraints.sql`), en vez de convertir a enum de Postgres para no romper el tipo Go (`string`) que sqlc genera para esas columnas — se optó por `CHECK` sobre el `varchar` existente por ser el cambio menos invasivo. Probado up/down/up contra Postgres real y verificado que un `INSERT` con un `status` inválido es rechazado por la constraint; `sqlc generate` (versión pineada en CI, `1.27.0`) corrido después sin diffs pendientes
- [x] Migración: `users.google_id varchar unique nullable` + `users.password_hash` nullable (`migrations/00002_auth.sql`, `docs/database/schema.dbml` actualizado) + tabla `refresh_tokens` (`token_hash` único, `expires_at`, `revoked_at`)
- [x] Constraint a nivel de BD: `CHECK (password_hash IS NOT NULL OR google_id IS NOT NULL)` en `users`
- [x] Diagrama ER visual exportado a `docs/diagrams/` (`docs/diagrams/er-diagram.md`, Mermaid `erDiagram` con las 11 tablas del esquema real, atributos, PK/FK/UK y notas de los índices/constraints de `00003`/`00004`; sintaxis validada renderizando con `@mermaid-js/mermaid-cli`)
- [x] Estrategia de migraciones futuras (naming, cómo versionar cambios de estadísticas pre-calculadas) — [ADR-0011](../decisions/0011-estrategia-migraciones-y-recalculo-estadisticas.md): formaliza el naming ya usado de facto (`%05d_slug.sql`, secuencia plana, `Up`/`Down` simétricos) y propone (sin implementar todavía) un comando `recalculate-stats` para re-derivar `user_statistics_summary`/`deck_statistics_summary` si cambia la fórmula de agregación, señalando la invariante que debe respetar (`RecalculateForGame` es incremental, no idempotente — cada partida debe procesarse exactamente una vez)

## Stage 3: API (contrato OpenAPI)

- [x] Esqueleto OpenAPI 3.1 con los paths principales
- [x] `requestBody` + schemas completos de auth: `RegisterRequest`, `LoginRequest`, `GoogleLoginRequest`, `RefreshRequest`, `LogoutRequest`, `TokenResponse` (con sus códigos de error: 401/400/409/501 según corresponda)
- [x] `requestBody` + schemas completos de `/decks` (`CreateDeckRequest`) y `/decks/import/moxfield` (`ImportMoxfieldRequest`), con 400/404
- [x] `requestBody` + schemas completos de `/games` (`CreateGameRequest`) y `/games/{id}/join` (`JoinGameRequest`), con 400/404/409
- [x] `requestBody` + schemas completos de `/playgroups` (`CreatePlaygroupRequest`) y `/playgroups/{id}/members` (`AddMemberRequest`, path que ni siquiera estaba documentado antes), con 400/404/409
- [x] Documentar `POST /auth/google` en el OpenAPI (request `{ id_token: string }`, respuesta igual a `/auth/login`: `TokenResponse`)
- [x] Schemas de respuesta de estadísticas: `UserStatsResponse`, `DeckStatsResponse`, `PlaygroupStatsResponse`/`PlaygroupMemberStats`, ya modelados y referenciados en los 3 endpoints de `/statistics/*`
- [x] Endpoint `/statistics/playgroup/{id}` implementado de verdad (antes devolvía `501` hardcodeado sin importar qué dijera el servicio)
- [x] Paginación cursor-based en listados (`/games`, `/decks`) — ya implementada (commit `6b0ddd5`, ajeno a esta sesión, TASKS.md nunca se había actualizado): `internal/common/pagination.go` (keyset sobre `created_at`+`id` DESC, `EncodeCursor`/`DecodeCursor`/`ParsePageRequest`), wireado en `games`/`decks` (`ListGamesPage`/`ListDecksPage`), documentado en `openapi.yaml` (`CursorParam`/`LimitParam`, `GameListResponse`/`DeckListResponse`). Gap real que sí queda: `GET /games/{id}/timeline` y los listados de `playgroups` siguen sin paginar/sin límite — no forma parte de este ítem, se deja como mejora futura si hace falta
- [x] Lint del spec con Spectral (`.spectral.yaml`, corre en `docs-ci.yml`) — 0 errores; quedan 93 warnings preexistentes en todo el spec (`operationId`/`tags`/`description` faltantes en casi todos los paths), no bloqueantes, pendientes como deuda de documentación

## Stage 4b: Cliente Web (Nuxt)

- [x] Decisión tomada 2026-07-26, actualizada 2026-07-27: agregar un segundo cliente, un **frontend web con Nuxt 4 + Tailwind**, 100% desacoplado del backend (solo consume la API REST, igual que Android), para import de decks de Moxfield y ver estadísticas post-partida. Vive en `web/` en la raíz del repo (al lado de `android/` y `backend/`), con **npm** como gestor de paquetes, en modo **SSR** (no SPA pura — requiere su propio proceso Node/Nitro corriendo en runtime, a diferencia de la extinta `tools/auth-test/`, que era estática). ADR formal: [ADR-0004](../decisions/0004-web-client-nuxt.md) (Nuxt 3 → Nuxt 4: criterio del proyecto de arrancar cada dependencia nueva en su versión mayor más actualizada)
- [x] **Esqueleto inicial scaffoldeado** (2026-07-27, migrado de Nuxt 3 a Nuxt 4 el mismo día), a pedido explícito del usuario adelantando el orden original (el ADR proponía esperar a que el backend de partidas/estadísticas estuviera terminado; se decidió arrancar ya solo con auth para poder levantar el proyecto):
  - Nuxt 4.5.0 + `@nuxtjs/tailwindcss`, npm, SSR (`nuxt build`/`nuxt dev` verificados localmente, sin SPA). Código de la app bajo `web/app/` (srcDir por defecto de Nuxt 4)
  - `app/pages/login.vue`: form email/password + botón de Google Sign-In (Google Identity Services, mismo flujo que la extinta `tools/auth-test/` pero como composable Vue: `app/composables/useGoogleIdentity.ts`)
  - `app/pages/index.vue`: única pantalla protegida por ahora, muestra el usuario autenticado (`GET /auth/me`) y permite cerrar sesión
  - `app/composables/useAuth.ts`: `login`/`loginWithGoogle`/`logout`/`fetchMe` contra `POST /auth/login`, `/auth/google`, `/auth/logout`, `GET /auth/me`; sesión (access/refresh token) en cookies vía `useCookie` (no `httpOnly` todavía — pendiente de endurecer si hace falta mitigar XSS)
  - `app/middleware/auth.global.ts`: redirige a `/login` sin sesión y fuera de `/login` con sesión, verificado en SSR (`GET /` con cookies vacías responde 200 tras redirect a `/login`)
  - Fuera de alcance de esta pasada: import de decks de Moxfield y estadísticas (siguen esperando el motor de partida + estadísticas reales, ver nota original más abajo), registro de usuario nuevo (solo login, no hay `pages/register.vue`), refresh automático de token expirado (el composable expone `logout`/`fetchMe` pero no reintenta con `/auth/refresh` todavía)
- [x] **Cliente web completo hasta el alcance de esta etapa** (2026-07-27). Todo lo que quedaba pendiente arriba está hecho y verificado end-to-end contra el backend real (`docker compose up`), tanto en `nuxt dev` como en el build SSR de producción (`node .output/server/index.mjs`):
  - [x] **Registro de usuario nuevo**: `app/pages/register.vue` + `register()` en `useAuth.ts`. Como `POST /auth/register` devuelve el `User` pero no tokens, el handler de Nitro encadena un `POST /auth/login` con las mismas credenciales y deja la sesión iniciada (el usuario no escribe la password dos veces). 409 de username/email duplicado se muestra en el form
  - [x] **Tokens en cookies `httpOnly` vía Nitro (BFF)**: el navegador ya no habla con la API Go ni ve los tokens. `server/api/auth/{register,login,google,logout,session}` son los únicos que tocan cookies de sesión, y `server/api/backend/[...path].ts` es un proxy autenticado que inyecta el `Authorization: Bearer` desde la cookie (con `auth/*` bloqueado, para que ningún camino desde JS devuelva un token). Se agrega una tercera cookie **no** `httpOnly`, `cc_session`, que solo contiene el marcador `"1"`: el middleware de rutas necesita decidir si hay sesión en SSR y en cliente, y falsificarla solo consigue un render que muere en el primer 401. Efecto lateral: el flujo normal del cliente web ya no necesita CORS
  - [x] **Refresh automático de sesión expirada**: `backendFetch` (`server/utils/backend.ts`) reintenta una vez contra `POST /auth/refresh` ante un 401, en el mismo espíritu que el `AuthAuthenticator` de OkHttp en Android. Como el backend **rota** el refresh token, hay dos cuidados: los refresh concurrentes se deduplican en memoria, y en SSR `app/composables/useNitroFetch.ts` mantiene un cookie jar por request (propaga los `Set-Cookie` de las llamadas internas a la respuesta real **y** a las llamadas siguientes del mismo render, que si no irían con un refresh token ya revocado)
  - [x] **Import de Moxfield**: `app/pages/decks.vue` acepta URL completa o ID público contra `POST /decks/import/moxfield`, muestra el deck creado (nombre + comandante) y lista los decks guardados. 404 (no existe deck público) y 400 (URL inválida / sin comandante, o sea no es formato Commander) se traducen a mensajes accionables en `moxfieldImportError()`
  - [x] **Estadísticas**: `app/pages/statistics.vue` consume `GET /statistics/user` y, por cada deck propio, `GET /statistics/deck/{id}`; `app/pages/index.vue` es un dashboard con el resumen. `GET /statistics/playgroup/{id}` quedó expuesto en `useStatistics()` sin pantalla hasta que existieron las páginas de playgroups (ver más abajo, 2026-07-28): ahora `app/pages/playgroups/index.vue` lo usa para mostrar partidas jugadas por grupo, y `[id].vue` para el desglose ganadas/jugadas por miembro
  - [x] Estructura de app: `app.vue` con `<NuxtLayout>`, `layouts/default.vue` (nav + logout), `components/StatCard.vue`, `types/api.ts`
  - [x] Tooling: ESLint (`@nuxt/eslint`) y typecheck (`vue-tsc`) agregados, ambos limpios (`npm run lint`, `npm run typecheck`)
- [x] `.github/workflows/web-ci.yml` (2026-07-27): mismo patrón que `backend-ci.yml`/`android-ci.yml` — job `changes` con `dorny/paths-filter` sin filtro de path + `needs`/`if` en el resto, para que siempre reporte un check y pueda marcarse como required. Jobs: `eslint + typecheck + nuxt build` y `hadolint` sobre `web/Dockerfile` (verificado localmente, sale limpio)
- [ ] Orden original acordado con el usuario (ya adelantado arriba): las pantallas de estadísticas muestran ceros hasta que haya partidas finalizadas reales (ver nota de alcance en Stage 7 y en "Wiring real" más abajo)
- [x] **Pantallas de playgroups en el cliente web** (2026-07-28): `app/pages/playgroups/index.vue` — listado de los grupos propios (nombre, cantidad de miembros, partidas jugadas vía `GET /statistics/playgroup/{id}`, best-effort: 404 de un grupo sin partidas se trata como cero) + modal de creación (`POST /playgroups`). `app/pages/playgroups/[id].vue` — detalle de un grupo: renombrar (`PATCH /playgroups/{id}`), estadísticas por miembro, agregar miembro con el autocomplete de `GET /users/search` (ver bullet de arriba), e historial de partidas del grupo (`GET /games?playgroup_id=`, vía `useGames.ts` nuevo). `usePlaygroups.ts` nuevo (`listPlaygroups`/`getPlaygroup`/`createPlaygroup`/`updatePlaygroup`/`addMember`)
- [x] **Pantalla de Ajustes de cuenta** (`app/pages/settings.vue`, `useSettings.ts`, 2026-07-28): cambiar password (`POST /users/{id}/password`, valida el actual, rechaza cuentas de Google sin password propio con un mensaje distinto al de "contraseña incorrecta"), vincular/editar el username de Moxfield (`PATCH /users/{id}`, reusa `AuthUser.moxfield_username` — campo nuevo en `useAuth.ts`/`server/utils/backend.ts`) y disparar el import masivo de decks en background (`POST /moxfield-import` + polling de `GET /moxfield-import/{id}` cada 2s mientras esté `in_progress`; el propio endpoint de import ya existía, ver Stage 8 — esta pantalla es la primera UI que lo expone). `app/pages/index.vue` gana un `refresh()` en `onMounted` para no mostrar el resumen cacheado de una sesión previa (p. ej. al volver de jugar una partida en Android)
- [x] **Internacionalización con `@nuxtjs/i18n`** (2026-07-29, PR #49) — [ADR-0014](../decisions/0014-internacionalizacion-web.md): extrae los ~200-250 literales hardcodeados de las 9 páginas, el layout y los composables de error a `web/i18n/locales/es.json` (un único locale `es` activo por ahora, `strategy: 'no_prefix'`, sin selector de idioma todavía — infraestructura lista para sumar `en`/`ca` más adelante sin tocar componentes). Namespacing por página/composable (`login.*`, `settings.*`, `playgroups.list.*`, `errors.*`, `toast.*`, `common.*`), pluralización/interpolación nativas de vue-i18n reemplazando los ternarios manuales que había. De paso corrige el copy de voseo argentino a tuteo de España, que queda como criterio de estilo para texto nuevo en la web. `web/i18n/i18n.config.ts` nuevo (`datetimeFormats`)
- [x] Botón de Google Sign-In personalizado en `login.vue` (2026-07-29, mismo PR #49): pasa de tema `outline` fijo (blanco, rectangular, 320px) a forma `pill`, ancho igual al del contenedor y tema dependiente del tema de la app (`filled_black` en oscuro, `outline` en claro) — antes desentonaba con la tarjeta oscura y los inputs en pill del resto del login
- [x] `<title>` por defecto + `titleTemplate` en Nuxt (2026-07-29, mismo PR #49): antes no se seteaba ningún título (violación WCAG 2.4.2 marcada por axe/Lighthouse, HTML sin `<title>`); las páginas que setean su propio título vía `useHead` ahora lo componen como "X · Commander Companion"
- [x] **Improve: layout de la imagen del deck + botón "ver en Moxfield"** (2026-07-30, reportado por el usuario): el overlay de `decks.vue` era un degradado vertical (arriba→abajo) con el texto anclado abajo, tapando buena parte de la imagen del deck. Cambiado a degradado horizontal (negro sólido a la izquierda, transparente a la derecha) con el bloque de texto reubicado a la izquierda, ocupando toda la altura de la card (`inset-y-0 left-0`, ancho ~60-68%) — la imagen queda visible completa a la derecha. `DeckArt.vue` gana una prop `imagePosition` (`'center' | 'right'`, default `'center'` para no tocar otros usos del componente) que ajusta el `object-position` del `<img>` para favorecer el lado derecho de la fuente al recortar con `object-cover`. Botón nuevo "Ver en Moxfield" (`https://moxfield.com/decks/{moxfield_id}`, `target="_blank"`) junto al de sincronizar, usando `Deck.moxfield_id` que ya se traía pero solo se usaba para gatear el botón de sync
- [ ] Mejoras visuales de la web (agregado 2026-07-27): pulir estilos y consistencia visual del cliente Nuxt (hoy funcional pero sin pasada de diseño dedicada) — sin alcance detallado todavía

## Stage 4: Cliente Android (base)

- [x] Proyecto inicializado: Compose, Material 3, Navigation, Hilt, Room, Retrofit, kotlinx.serialization
- [x] Theming base (`Color.kt`, `Theme.kt`, `Type.kt`) y navegación con rutas (`AppNavigation.kt`, `Routes.kt`)
- [x] **Life tracker local completo y funcional** (2026-07-26), 100% en memoria + Room, sin depender del backend:
  - `PlayerSetupScreen`: elegir 2-6 jugadores, nombre y color (paleta WUBRG + incoloro) por jugador
  - `GameTrackerScreen`: grid dinámico (filas de hasta 2 cartas, funciona para 2-6 jugadores en vez del layout fijo de 4 anterior), contador de turno, vida ±, panel de daño de comandante por oponente (`PlayerCard`) que también resta vida
  - `GameViewModel`: fin de partida automático cuando solo queda 1 jugador con vida > 0, o manual con botón "Finalizar" + diálogo de confirmación; detecta ganador (o empate si hay más de un jugador con la vida máxima)
  - Persistencia real en Room: `GameEntity`/`PlayerResultEntity`/`GameWithPlayers` (`DatabaseModule` nuevo en Hilt) — la partida se guarda al crearse (`IN_PROGRESS`) y se actualiza al finalizar (`FINISHED` + vida final + ganador)
  - `HistoryScreen`: lista de partidas pasadas (fecha, nº de jugadores, estado, vida final y color de cada jugador) leída de Room vía `HistoryViewModel`
  - Verificado end-to-end en emulador (`Pixel_10_Pro`): setup → tracker → daño de comandante → finalizar → historial persistido
  - Deliberadamente fuera de alcance de esta pasada: recuperación de partida en curso tras kill del proceso (el estado de vida vive solo en memoria hasta finalizar), autenticación, y cualquier llamada al backend — sigue siendo 100% local
- [x] **Pantalla de pre-partida: sorteo de turno + mulligans** (`PreGameScreen`, 2026-07-26), insertada entre `PlayerSetupScreen` y `GameTrackerScreen`:
  - Randomizador "¿Quién empieza?": elige al azar uno de los jugadores configurados (`Random.nextInt`), lo resalta con su color y el resultado viaja a `GameTrackerRoute` (`startingPlayerSeat`) para mostrar el badge "· empieza" en su `PlayerCard` durante toda la partida
  - Contador de mulligans por jugador (± , sin límite superior, mínimo 0) antes de la primera mano; se guarda en `PlayerState.mulligans` y se persiste en `PlayerResultEntity.mulligans` (columna nueva) — visible tanto en el tracker (badge bajo el nombre si > 0) como en el historial (`"vida (Nm)"`)
  - `PlayerConfig`/`PlayerConfigCodec` extendidos con un tercer campo `mulligans` (formato `name|colorKey|mulligans`, decodificación retrocompatible con encodes de 2 campos) para llevar el dato de `PlayerSetupScreen` → `PreGameScreen` → `GameTrackerScreen` sin más estado compartido que la propia ruta
  - **Gotcha real encontrado y corregido**: añadir la columna `mulligans` a `PlayerResultEntity` sin subir `CommanderDatabase` de `version = 1` a `version = 2` crashea la app en cualquier dispositivo con una instalación previa (`Room cannot verify the data integrity` — `fallbackToDestructiveMigration` solo se dispara ante un cambio de versión, no ante un cambio de esquema en la misma versión). Bump de versión aplicado; queda como recordatorio para futuros cambios de entidades Room
  - Verificado end-to-end en emulador: sorteo → mulligans → tracker (badges correctos) → finalizar → historial con mulligans persistidos
- [x] Pantallas placeholder: `DashboardScreen` (ahora con navegación real a setup/historial), componente `PlayerCard` (ahora con lógica real de daño de comandante)
- [x] **Flujo de navegación de la app definido** (decisión, 2026-07-26; actualizado con `PreGameRoute`): `LoginRoute → DashboardRoute → (PlayerSetupRoute → PreGameRoute → GameTrackerRoute → vuelta a Dashboard) / HistoryRoute`. `LoginRoute` es ahora el `startDestination` del `NavHost`. Decisiones tomadas:
  - Se implementa el login como **shell de navegación primero, sin autenticar todavía**, para fijar la estructura de rutas antes de conectar el backend real — evita tener que reestructurar el grafo de navegación (y las pantallas que ya asumen "usuario logueado") más adelante
  - Los dos métodos de auth que ya soporta el backend (`POST /auth/login` y `POST /auth/google`) tienen **callbacks separados desde ya** en `LoginScreen` (`onLoginWithPassword`, `onLoginWithGoogle`), diferenciados también visualmente (botón sólido vs. botón outline + separador "o"), aunque hoy ambos solo navegan a `DashboardRoute` — conectar cada uno a su endpoint real será un cambio acotado en el callback, no un rediseño de pantalla
  - Al llegar a `DashboardRoute` se hace `popUpTo(LoginRoute) { inclusive = true }`: una vez "dentro" de la app el botón atrás no debe volver al login
  - El life tracker seguía siendo 100% local (Room) independientemente del login: no había ninguna llamada al backend todavía, ni desde el login ni desde la partida — eran dos piezas construidas en paralelo que se conectarían juntas cuando `CommanderApi.kt` tuviera endpoints reales. **Actualizado 2026-07-27: ya se conectaron** — ver más abajo (`CommanderApi.kt` con 15 endpoints reales, `GameRepository`/`DeckRepository`, espejo best-effort del asiento local contra `games`/`game-actions`)
- [x] Pantallas de autenticación (login/registro) — `LoginScreen` ahora autentica de verdad contra el backend vía `LoginViewModel` (2026-07-27):
  - [x] Dependencia Credential Manager + Google Identity Services (`androidx.credentials`, `androidx.credentials:credentials-play-services-auth`, `com.google.android.libraries.identity.googleid`)
  - [x] `onLoginWithGoogle` conectado al flujo real de Credential Manager (`GoogleAuthClient.getIdToken`) para obtener el `id_token` — **caveat**: las credenciales OAuth reales de Google Cloud (Web Client ID) todavía no existen (paso manual externo, ver Stage 1); `BuildConfig.GOOGLE_WEB_CLIENT_ID` es un placeholder documentado en `app/build.gradle.kts`, así que el flujo está completo e implementado correctamente pero no se pudo verificar end-to-end contra Google real. En cuanto se cree el Client ID real, basta con pasarlo por `-PGOOGLE_WEB_CLIENT_ID=...` o `gradle.properties`, sin tocar código
  - [x] `onLoginWithPassword`/`onLoginWithGoogle` conectados a `POST /auth/login` / `POST /auth/google` (`AuthApi.kt` nuevo, separado de `CommanderApi.kt`) y los tokens devueltos se guardan en `SessionManager` (DataStore)
  - [x] Manejo de estado en `LoginViewModel`/`LoginScreen`: credenciales inválidas (401), usuario cancela el picker de Google (`GoogleSignInCancelledException`, no muestra error), sin cuenta Google en el dispositivo (`NoGoogleAccountException`), backend rechaza el token de Google (400/501) y error de red (`IOException`)
  - [x] Flujo de "Cerrar sesión" (`DashboardScreen` → `DashboardViewModel.logout()` → `SessionManager.logout()`): revoca el refresh token contra `POST /auth/logout` (best-effort), limpia DataStore y llama `clearCredentialState` de Google
- [ ] Capa de dominio (`domain/` con use cases e interfaces de repositorio) — sigue sin existir, se salta directo de repositorio/API a `ViewModel` (`GameViewModel`/`HistoryViewModel` ya van contra `GameRepository`, ver ítem de repositorios abajo; `LoginViewModel`/`DashboardViewModel` siguen inyectando `AuthApi`/`SessionManager` directo, sin repositorio propio de auth); aceptable para el alcance actual, revisar si se justifica más adelante
- [x] **Repositorios reales en `data/repository/`** (2026-07-27, antes no existían — `GameDao` se inyectaba directo en `GameViewModel`/`HistoryViewModel`): `GameRepository` es el punto único que decide qué va a Room (historial local, `persistNewLocalGame`/`persistLocalResult`, siempre) y qué va al backend (`bootstrapRemoteGame`/`recordLifeChange`/`finishGame`, espejo best-effort del asiento local, ver detalle en Stage 5); `DeckRepository` es 100% remoto (`listDecks`/`createDeck`/`importFromMoxfield`/`deleteDeck`). `GameViewModel`/`HistoryViewModel` ya no inyectan `GameDao` directo, van contra `GameRepository`
- [x] DI de Room: `DatabaseModule` (Hilt) provee `CommanderDatabase` y `GameDao`. **Módulo de red agregado** (2026-07-27): `NetworkModule` (Hilt) provee Retrofit/OkHttp — `AppModule` seguía proveyendo solo `Context`, ahora la red vive en su propio módulo
- [x] `GameViewModel.kt` — ya no está vacío: vida, turno, daño de comandante y persistencia del resultado en Room (ver nota del life tracker más arriba)
- [x] **`CommanderApi.kt` con endpoints reales** (2026-07-27, antes solo tenía `GET /health`): 15 métodos — `decks` (list/create/get/delete/import Moxfield), `games` (list/create/get/join/leave/start/finish), `game-actions` (recordAction/getTimeline), `statistics` (user/deck/playgroup). Usa el cliente HTTP autenticado de `NetworkModule` (`AuthInterceptor` + `AuthAuthenticator`, ver Stage 5). Los repositorios (`GameRepository`/`DeckRepository`) envuelven cada llamada con `apiCall { }` para traducir `HttpException`/`IOException` a `ApiError`
- [x] `AuthApi.kt` nuevo (`data/remote/api/AuthApi.kt`), separado de `CommanderApi.kt` a propósito (evitar conflictos de merge con quien extienda decks/games/etc. en paralelo): cubre `login`/`google`/`refresh`/`logout`/`me` de `docs/api/openapi.yaml`, con sus DTOs en `data/remote/dto/AuthDto.kt`
- [x] `GameState.kt` — modela vida, turno, daño de comandante por oponente y **veneno** (`PlayerState.poison`, ver bullet de rediseño más abajo) para N jugadores (2-6). Energía/experiencia siguen sin modelar (no forman parte del alcance de esta pasada)
- [x] **Rediseño visual completo con estética violeta + flujo de registro** (2026-07-30, PR #50), siguiendo un mockup HTML de referencia (`Commander Companion Android.dc.html`):
  - Reimplementadas `LoginScreen`, `DashboardScreen`, `HistoryScreen`, `PlayerSetupScreen`, `PreGameScreen` y `GameTrackerScreen` sobre una paleta violeta/oscura nueva (`theme/Color.kt`, `theme/Gradients.kt` nuevo: `AccentGradient`/`TitleGradient`/`ScreenBackgroundGradient`) y una librería de componentes compartidos nueva, `presentation/components/AppComponents.kt` (`GradientButton`/`GradientOutlineButton`, `GlassCard`, `PillSegmentedControl`, `SelectableChip`/`SelectableCircle`, `AuthTextField`, `StatusPill`, `RotateDevicePrompt`, etc.), que reemplaza a `PlayerCard.kt` (eliminado — su lógica de tarjeta de jugador vive ahora como `PlayerQuadrant`, privado de `GameTrackerScreen.kt`; `android/README.md` actualizado)
  - `PreGameScreen`/`GameTrackerScreen` pasan a requerir **orientación horizontal** (`RotateDevicePrompt` si el dispositivo está en vertical) con una grilla de asientos (`QuadrantGrid`) en vez del layout de filas anterior; se agregan botón/overlay de pausa (`PauseButton`/`PauseOverlay`) y un resumen final de partida (`GameSummary`, daño hecho/recibido y ganador por jugador) — ninguno existía antes
  - **Contador de veneno real** (no solo visual): `PlayerState.poison` nuevo; `PlayerState.isEliminated()` centraliza las 3 condiciones de eliminación (vida ≤0, veneno ≥10, daño de comandante ≥21 de un mismo oponente) — antes solo vida y daño de comandante estaban chequeados, en un `isAlive` privado de `GameViewModel` que no conocía el veneno. `GameViewModel.adjustPoison` + `GameRepository.recordPoisonChange` espejan el cambio al backend como `PoisonCounter` (mismo patrón best-effort del asiento local que `LifeChange`/`CommanderDamage`, ver Stage 5). De paso se eliminó `GameViewModel.previousTurn()` (ya no se puede retroceder el contador de turno)
  - **Flujo de registro nuevo** (no existía en Android, solo en el cliente web): `RegisterScreen`/`RegisterViewModel` nuevos (`presentation/screens/register/`), `RegisterRoute` agregado a la navegación (accesible desde `LoginScreen`). Llama a `POST /auth/register` (`AuthApi.register`, nuevo — la interfaz solo tenía `login`/`google`/`refresh`/`logout`/`me`) con el mismo contrato que ya usa el cliente web: el registro **no deja sesión iniciada** (el backend devuelve el `User` creado, no tokens), así que la pantalla muestra "revisá tu email" en vez de navegar al dashboard. Validación client-side de longitud mínima de password (8 caracteres) y mapeo de errores (409 = usuario/email duplicado, 400 = datos inválidos, resto = mensaje genérico con el código)
  - Sin tests nuevos para `RegisterViewModel` ni para los componentes de `AppComponents.kt` — gap conocido, no cerrado en esta pasada (ver ítem Transversal de tests de Android, que sigue sin mencionar `register`)
- [x] Mejora de la UI de la app Android (agregado 2026-07-27) — **hecho 2026-07-30** vía el rediseño violeta descrito arriba (paleta, componentes compartidos, grilla de asientos, pausa, resumen final)

## Stage 5: Integración Android ↔ Backend

- [x] Interceptor de autenticación (2026-07-27): `AuthInterceptor` adjunta el Bearer access token a las requests del cliente HTTP autenticado (`NetworkModule`, usado por `CommanderApi`); `AuthAuthenticator` intercepta los 401, llama a `POST /auth/refresh` una vez y reintenta la request original con el token nuevo. Válido tanto para sesiones de password como de Google (ambas guardan el mismo `TokenResponse` en `SessionManager`). Si el refresh falla, `SessionManager` fuerza logout (limpia DataStore + `clearCredentialState`) y emite un evento (`forcedLogoutEvents`) que `AppNavigation`/`SessionViewModel` escuchan para volver a `LoginRoute` desde cualquier pantalla. Nota de diseño: `AuthApi` (login/google/refresh/logout) usa un cliente OkHttp *sin* este interceptor a propósito — esos endpoints son públicos (`security: []` en el spec) y reusar el cliente autenticado para el propio refresh causaría una recursión
- [x] Persistencia de sesión con DataStore (2026-07-27): `SessionManager` (`data/session/SessionManager.kt`) guarda access token, refresh token y expiry (`androidx.datastore:datastore-preferences`, agregado a `app/build.gradle.kts`)
- [x] **Espejo best-effort del asiento local contra el backend real** (2026-07-27, selección de asiento/deck explícita agregada 2026-07-28): `GameRepository.bootstrapRemoteGame(deckId)` — al crear una partida, `POST /games` + `POST /games/{id}/join` con el deck que el usuario eligió en `PlayerSetupScreen` para el asiento marcado "Soy yo" + intento de `POST /games/{id}/start` (409 = "todavía no hay 2 jugadores", no es error, la sesión queda `PENDING`); `GameViewModel.mirrorLifeChange` espeja cada cambio de vida del asiento local vía `POST /games/{id}/actions` (`LifeChange`, sin `target_id`) mientras la sesión esté `active`; `finishRemoteGame` dispara `POST /games/{id}/finish` al terminar. Deliberadamente **best-effort y aditivo**: sin red/sesión/deck elegido, o si la partida nunca llega a 2 jugadores desde el backend, el tracker local sigue jugándose igual — el estado se refleja en `GameState.remoteSync`/`RemoteSyncBanner` (`Disabled`/`WaitingForPlayers`/`Synced`/`Failed`), nunca bloquea. Serializado con un `Mutex` (`remoteMutex`) para que las operaciones remotas salgan en el mismo orden que la UI las generó. Tests: `GameRepositoryTest.kt`, `GameViewModelTest.kt` (estados de sync), `PlayerConfigCodecTest.kt`, `ApiCallTest.kt`. **Límite explícito**: solo el asiento marcado como local (usuario autenticado) tiene `GamePlayer` remoto; los demás asientos y el daño de comandante de cualquier asiento nunca se espejan (comentario dedicado en `GameViewModel.adjustCommanderDamage` explicando por qué)
- ~~[x] **Selección explícita de "quién soy" y de deck en `PlayerSetupScreen`** (2026-07-28): chip "Soy yo" por asiento + picker del propio deck; `PlayerConfig.isLocalUser`/`deckId`.~~ **Superada el mismo día** por el modo Grupo (ver bullet siguiente): "soy yo" era un caso particular de "asignar un asiento a un usuario real" — el modo Grupo lo generaliza a cualquier miembro del playgroup, así que `isLocalUser` se eliminó de `PlayerConfig` en vez de mantenerse en paralelo.
- [x] **Modo Casual/Grupo en `PlayerSetupScreen`, con proxy-join real** (2026-07-28): pedido explícito del usuario tras notar que no había forma de "compartir la partida" con su grupo real. Casual: como el life tracker de siempre, garantizado sin red (si no hay ningún asiento asignado, `GameRepository.bootstrapRemoteGame` ni siquiera crea la partida remota). Grupo: elegís un playgroup (`PlaygroupRepository.listPlaygroups()`, con miembros ya poblados) y por cada asiento elegís "Invitado" o un miembro del grupo — al elegir un miembro se carga su lista de decks (`GET /playgroups/{id}/members/{userId}/decks`, on-demand y cacheada en `PlayerSetupViewModel`) para elegir con cuál juega. Al empezar, `GameRepository.bootstrapRemoteGame(playgroupId, assignments)` crea la partida (con `playgroupId`) y hace un `join` por cada asiento asignado — self-join o proxy-join según decida el backend (ver ADR-0013) — devolviendo `RemoteGameSession.seatPlayerIds: Map<Int, String>` en vez de un único `localPlayerId`. `GameViewModel.mirrorLifeChange`/`mirrorCommanderDamage` espejan la acción de CUALQUIER asiento presente en ese mapa, no solo "el mío": esto resuelve el TODO explícito que tenía `adjustCommanderDamage` sobre no poder atribuir daño de un asiento sin `GamePlayer` propio — ahora si ambos asientos (atacante y defensor) están en el mapa, se manda `CommanderDamage` con `actor_id`/`target_id` reales. `PlayerConfig` pasó de `isLocalUser` a `assignedUserId`/`assignedUsername`/`deckId` (sin necesidad de saber "cuál asiento soy yo": el backend lo decide comparando `assignedUserId` contra el JWT). Tests: `GameRepositoryTest.kt` (bootstrap multi-asiento, `recordCommanderDamage`), `GameViewModelTest.kt` (mirror de daño de comandante entre dos asientos asignados, y su ausencia si el atacante es invitado), `PlaygroupRepositoryTest.kt` nuevo, `PlayerConfigCodecTest.kt` actualizado.
- [ ] Flujo end-to-end **completo**: lo de arriba cubre create→join→start→actions→finish para un solo dispositivo/asiento; falta que **múltiples dispositivos** se unan a la **misma** partida real (hoy cada instalación de la app crea la suya propia) y ver en vivo lo que hacen los demás (Stage 6, cliente Websocket). **Parcialmente mitigado** (2026-07-28) para el caso de un grupo real jugando junto: el modo Grupo de Android usa proxy-join (ver ADR-0013 y la entrada de Playgroups) para que un solo dispositivo siente a varios miembros del mismo playgroup con stats reales para todos — sigue siendo un solo dispositivo anotando, no sync en vivo entre dispositivos separados.
- [ ] Room como caché offline-first (partidas vistas, decks propios) con estrategia de sincronización — `DeckRepository` sigue siendo 100% remoto, sin caché

## Stage 6: Sincronización (Websocket)

- [x] Diseño del protocolo de mensajes (qué eventos de `game_actions` se retransmiten en vivo y su formato) — [ADR-0005](../decisions/0005-websocket-protocol.md): retransmite las 7 acciones de `game_actions` sin filtrar (mismo DTO `GameActionResponse` que ya usa REST) + un evento de ciclo de vida `game_finished`; sobre JSON común (`type`/`game_id`/`actor_id`/`payload`/`timestamp`); autenticación por mensaje `{"type":"auth","token":"..."}` como primer frame tras conectar (no JWT por query param, para no filtrarlo en los logs de acceso de Fiber; no subprotocolo, por el uso semánticamente incorrecto del campo); documenta explícitamente fuera de alcance: replay/garantías de entrega al reconectar, autorización por membership del `game_id`, multi-proceso/pub-sub externo, presencia, canal cliente→servidor, heartbeat applicativo
- [x] Implementación del servidor en `internal/websocket/` (antes carpeta vacía): `Hub` en memoria (registro/broadcast/cierre de sala por `game_id`, protegido con `sync.RWMutex`), `Client` que envuelve la conexión de `github.com/gofiber/websocket/v2` (ya usado por `gofiber/fiber`, dependencia nueva agregada), handler de upgrade + autenticación (`auth.VerifyAccessToken`, nueva función exportada que reusa la verificación de JWT ya existente en `internal/auth/token.go` sin duplicarla) en `GET /api/v1/ws/games/:id` (ruta pública, sin `auth.RequireAuth`, ya que el handshake no puede llevar el header `Authorization`). Wireado sin dependencia dura: `games.Broadcaster`/`gameactions.Broadcaster` son interfaces definidas del lado del consumidor (mismo patrón que `games.StatisticsRecalculator`) que `*websocket.Hub` satisface; `game-actions.RecordAction` retransmite cada acción tras persistirla, `games.FinishGame` retransmite `game_finished` y cierra la sala. Tests unitarios puros del `Hub` (`internal/websocket/hub_test.go`: register/unregister, broadcast solo a la sala correcta, sala desconocida es no-op, `CloseRoom` cierra y limpia) sin necesidad de Postgres ni de un socket real (fake `Conn` en memoria). `go build`/`go vet`/`golangci-lint run` limpios (los 34 "File is not properly formatted (gofmt)" que golangci-lint reporta en el repo son un artefacto preexistente del checkout en Windows con `core.autocrlf` — afectan también archivos jamás tocados en esta pasada, ej. `internal/moxfield/client.go`; `internal/websocket/` en particular da 0 issues)
- [ ] Cliente websocket en Android (conexión, reconexión con backoff, aplicar eventos entrantes al `GameState`) — pendiente; consume el protocolo de [ADR-0005](../decisions/0005-websocket-protocol.md)

## Stage 7: Estadísticas

- [x] Lógica real de recálculo al finalizar partida — `games/service.go: FinishGame` dispara `statistics.RecalculateForGame`; ver detalle completo en Stage 1 ("Statistics — recálculo real y consultas")
- [x] Queries de agregación para `user_statistics_summary` y `deck_statistics_summary` (`UpsertUserStatistics`/`UpsertDeckStatistics`, upsert incremental con `ON CONFLICT DO UPDATE`)
- [x] `internal/statistics` service real: `GetUserStats`/`GetDeckStats` consultan la BD de verdad (con ownership check en deck y ceros por defecto si nunca jugó)
- [x] Servicio y endpoint de estadísticas por playgroup (`GetPlaygroupStats`, agregación en vivo sobre partidas finalizadas)
- [ ] UI de estadísticas en Android (no hay pantalla) — ya no bloqueado por falta de conexión al backend (Android ya habla con `games`/`game-actions`, ver Stage 4/5): simplemente falta la pantalla y el repositorio que llamen `CommanderApi.getUserStats`/`getDeckStats`/`getPlaygroupStats` (los tres métodos ya están en la interfaz). El cliente web (Nuxt) sí los consume ya (`app/pages/statistics.vue`)

## Stage 8: Importación Moxfield

- [x] Investigar la API pública/no oficial de Moxfield a integrar — `GET https://api2.moxfield.com/v3/decks/all/{publicId}`. Está detrás de Cloudflare: bloquea requests sin un `User-Agent` que parezca un navegador real (probado con `curl` — sin headers da la página de challenge de Cloudflare; con `User-Agent` de Chrome responde JSON normal, incluso sin `Referer`). El `publicId` es el segmento final de la URL del deck (`moxfield.com/decks/{publicId}`); los comandantes están en `boards.commanders.cards[*].card.name`.
- [x] Cliente HTTP hacia Moxfield en el backend — `internal/moxfield` (`Client.GetDeck`, `ExtractPublicID` para aceptar URL completa o solo el ID)
- [x] Reemplazar el stub de `internal/sync/service.go` (2026-07-27, commit `fae24bd`, TASKS.md nunca se había actualizado) — implementación real y **deliberadamente síncrona**: `TriggerSync`/`GetSyncStatus` delegan en `decks.Service` (`ResyncFromMoxfield`/`GetMoxfieldSyncState`), sin cola/job en background. El propio `service.go` (líneas 1-19) documenta por qué: una cola de verdad metería una dependencia dura nueva (broker + worker + tabla de jobs) que el resto del proyecto no tiene, para un trabajo del orden de cientos de milisegundos (una llamada HTTP + un UPDATE). `POST /sync/moxfield` responde 200 (no 202) porque el resync se aplica dentro del propio request. 5 tests reales en `internal/sync/service_test.go` contra un `mockResyncer`. La tarea de import **masivo y asíncrono** de todos los decks de un usuario (candidata real a background worker) sigue pendiente por separado, ver Stage 8 más abajo
- [x] Endpoint `POST /decks/import/moxfield` implementado end-to-end: resuelve el ID (URL o bare ID), llama a Moxfield, crea el deck con `name`/`commander` reales y `moxfield_id` seteado. 404 si el deck de Moxfield no existe, 400 si no tiene comandante (deck no es de formato Commander). Probado contra la API real (dos decks públicos distintos, uno vía URL completa y otro vía ID)
- [x] Imagen del deck importado (`decks.image_url`, migración `00006_deck_image_url.sql`): `internal/moxfield/client.go` toma el campo `main.id` de la respuesta de Moxfield (la carta que Moxfield destaca como portada del deck, normalmente el comandante) y arma `https://assets.moxfield.net/cards/card-{id}-art_crop.jpg` — el mismo art crop que Moxfield expone como su propio `og:image`. Persistido en `CreateDeck`/`UpdateDeckFromMoxfield`, expuesto en `DeckResponse.image_url` y mostrado como thumbnail en `web/app/pages/decks.vue` (deck importado y lista de decks guardados). Verificado end-to-end contra un deck público real (Playwright, captura del thumbnail renderizado)
- [x] Manejo de errores, rate limiting y reintentos ante la API externa (2026-07-27, PR #24) — `internal/moxfield/client.go: GetDeck` reintenta (3 intentos, backoff exponencial 200/400/800ms) ante errores transitorios (red/timeout, 5xx, 429), respetando `Retry-After` si Moxfield lo manda; un 404 nunca se reintenta. Al agotar los reintentos, el error se traduce a `common.UpstreamUnavailable` (→ 503 limpio) en vez de un 500 genérico con el error interno de Go filtrado al cliente (bug de leak cerrado en el mismo cambio, ver `internal/common/errors.go: ErrorHandler`)
- [x] Perfil de usuario: campo de usuario/username de Moxfield vinculado a la cuenta (2026-07-27) — `users.moxfield_username` (nullable, sin unicidad, `migrations/00009_moxfield_username.sql`). Implementado `PATCH /users/{id}` (antes documentado en `openapi.yaml` pero sin ningún backing code — spec fantasma), acotado a que `id` sea el propio usuario autenticado (404 si no, mismo criterio de "no revelar" que `decks`/`playgroups`). `GET /users/{id}` con un ID arbitrario sigue sin implementar (expondría perfiles ajenos sin necesidad real para esta feature) — gap conocido, no resuelto en esta pasada. Preparación para el import masivo en background (ver ítem siguiente)
- [x] Tarea en background para importar todos los decks del usuario de forma asíncrona (2026-07-27) — **scaffold completo, con el fetch real de Moxfield stubbeado**: `internal/moxfieldimport` nuevo (`POST /moxfield-import`, `GET /moxfield-import/{jobId}`), tabla `moxfield_import_jobs` (`migrations/00010_moxfield_import_jobs.sql`, índice único parcial que solo permite un job `pending`/`in_progress` por usuario → 409 en doble inicio). Primer uso de una goroutine desacoplada del ciclo de vida de un request en todo el proyecto (con `recover()` propio y `context.Background()`, ver el doc de paquete de `internal/moxfieldimport/service.go`) — **limitación explícita**: solo funciona para el despliegue de una sola instancia de hoy, un job no sobrevive a un restart a mitad de import. `MoxfieldClient.ListDecksByUsername` (`internal/moxfield/client.go`) es un **stub** que siempre devuelve un error mapeado a `501` — no se pudo investigar el endpoint real de Moxfield para listar los decks de un usuario (la política de red de este sandbox bloquea `api2.moxfield.com`, a diferencia de la investigación original del endpoint de un solo deck). `StartImport` resuelve la lista de forma síncrona antes de crear el job, así que hoy responde `501` limpio en el momento, sin crear ningún job — pendiente confirmar el endpoint real en un entorno con acceso de red antes de que la feature funcione de punta a punta. Todo lo demás (409 de import duplicado, progreso, finalización, éxito/falla parcial/falla total) es real y probado con un `MoxfieldClient` mockeado

## Stage 9: Social — amigos, grupos y torneos

- [ ] Sistema de amigos: enviar/aceptar/rechazar solicitudes, listado de amigos — distinto de los `playgroups` ya implementados en Stage 1 (que son grupos de partida, no relaciones de amistad)
- [ ] Agregar grupos más allá de los `playgroups` existentes, o extenderlos, según lo que requiera el flujo de torneos
- [ ] Creación de torneos: entre amigos, entre grupos, o abiertos a desconocidos que se apunten (inscripción abierta)
- [ ] Definir formato de torneo (bracket, suizo, liga, etc.) y el modelo de datos asociado antes de implementar

## Transversal (calidad, infraestructura, seguridad)

- [x] Tests unitarios de backend — tests de integración (Postgres real, `internal/testutil`) para `auth` (login, rotación/expiración/revocación de refresh tokens, cuentas Google-only, `Me`), `decks` (ownership check en `GetDeck`/`DeleteDeck`, listado por usuario, import de Moxfield con cliente mockeado), `games` (máquina de estados completa: join/leave solo en pending, ownership de deck al unirse, mínimo de jugadores para iniciar, transiciones inválidas → 409), `game-actions` (validación de `action_type`, mutación real de `life_total`/`poison_counters`, auto-eliminación a 0 de vida y a 10 de veneno, orden del timeline, partida no-activa → 409), `playgroups` (auto-join del creador, scoping por membresía, validaciones de `AddMember`) y `statistics` (recálculo end-to-end jugando partidas reales: ganador, sin-ganador, acumulación entre partidas, ownership, agregación por playgroup), más tests unitarios puros de `moxfield.ExtractPublicID`. `go test` corre con `-p 1` (los tests comparten la misma base y hacen `TRUNCATE` entre sí). **`users` (2026-07-27, commit `fae24bd`, TASKS.md nunca se había actualizado)**: `internal/users/service_test.go` (301 líneas, 15 tests) cubre `RegisterUser` (éxito, hash de password verificado directo en la columna, email/username duplicado), `GetUser` (éxito, 404, ID malformado), `VerifyCredentials` (éxito, password incorrecta, email desconocido) y `FindOrCreateGoogleUser` (alta, vinculación por email verificado, idempotencia, cuenta sin password, email no verificado rechazado, colisión de username resuelta) — ya no queda ningún módulo cubierto solo indirectamente
- [x] **Tests de Android** (2026-07-27; antes 0 tests) — unitarios puros con JUnit + `kotlinx-coroutines-test` (`android/app/src/test/`, fakes propios en `testing/Fakes.kt`, sin mocking framework): `ApiCallTest` (mapeo `HttpException`→`ApiError.Http`, `IOException`→`Network`, cualquier otra→`Unexpected`, `CancellationException` se relanza en vez de convertirse en failure, `toUserMessage`), `GameRepositoryTest` (`bootstrapRemoteGame` en orden create→join→start, 409 de start tratado como pending, error real propagado, sin decks devuelve éxito sin sesión, `recordLifeChange`/409 de partida no activa, persistencia local), `DeckRepositoryTest` (`firstDeckId` con/sin decks, propaga error de red, `importFromMoxfield`/404), `GameViewModelTest` (estados de `remoteSync`: `Synced` con partida activa, `Failed` con mensaje de conexión sin red). Corridos en CI por `android-ci.yml` (`testDebugUnitTest`)
- [x] CI/CD con GitHub Actions — quality gates creados en `.github/workflows/`:
  - `backend-ci.yml`: gofmt + go vet, golangci-lint, `sqlc generate` sin diffs pendientes, build + `go test -race` + migraciones goose contra Postgres real (servicio en el job), hadolint sobre `backend/Dockerfile`
  - `android-ci.yml`: Android Lint (`lintDebug`), tests unitarios (`testDebugUnitTest`), `assembleDebug`, publica reportes como artifact
  - `docs-ci.yml`: valida las fuentes de verdad — Spectral lint sobre `docs/api/openapi.yaml` (ruleset en `.spectral.yaml`) y `docs/database/schema.dbml` compilando a SQL con `@dbml/cli`. Se corrigió el step `dbml2sql`: `npx --yes @dbml/cli dbml2sql ...` fallaba con "could not determine executable to run" (con npm 10.8.2 el paquete expone 3 bins — `db2dbml`, `dbml2sql`, `sql2dbml` — y npx no puede desambiguar cuál correr sin `-p`); se cambió a `npx --yes -p @dbml/cli dbml2sql ...`
  - Repo vinculado a [github.com/mllagostera/commander-companion](https://github.com/mllagostera/commander-companion), `main` como rama por defecto
  - Los 4 workflows (`backend-ci.yml`, `android-ci.yml`, `web-ci.yml`, `docs-ci.yml`) siguen el mismo patrón `changes` (job con `dorny/paths-filter`) + `needs`/`if` por job, en vez de filtrar por `paths:` a nivel de `on:` — así siempre reportan un check (success/failure/skipped) sin importar qué archivos toque el PR. Necesario porque un *required check* que nunca se dispara (por el filtro de path) deja el PR bloqueado para siempre
  - [x] **Branch protection activado en `main`** vía `gh api PUT .../branches/main/protection`: exige los 8 checks a nivel de job (`gofmt / go vet`, `golangci-lint`, `sqlc generate (sin diffs pendientes)`, `build, test y migraciones contra PostgreSQL`, `hadolint (Dockerfile)`, `lint, unit tests y assembleDebug`, `Lint OpenAPI (Spectral)`, `Validar DBML (compila a SQL)`), rama actualizada (`strict: true`), `enforce_admins: true`, sin force-push ni delete de `main`. No se exige aprobación de PR (proyecto de un solo mantenedor). **`web-ci.yml` se agregó después** (ver Stage 4b) y no está en esta lista de checks requeridos todavía — pendiente decidir si se agrega (nota no obvia: su job `hadolint (Dockerfile)` comparte nombre con el de `backend-ci.yml`, así que hoy cualquiera de los dos satisface ese check requerido — GitHub matchea por nombre de job, no por workflow — pero `eslint, typecheck y nuxt build` no es requerido por nada)
  - Pendiente: decidir si se limpia `.github/modernize/java-upgrade` (no es de este proyecto; hoy no se sube al repo porque tiene su propio `.gitignore` con `**/*`)
  - [x] **`web-ci.yml`** (2026-07-27, antes no existía): mismo patrón que los otros tres — job `eslint, typecheck y nuxt build` (`npm run lint -- --max-warnings=0`, `npm run typecheck`, `npm run build`) y job `hadolint (Dockerfile)` sobre `web/Dockerfile`
  - Nota: el job `lint` ya debería pasar en verde — el backlog de golangci-lint que exponía se resolvió (ver ítem siguiente)
- [x] `golangci-lint` ejecutable localmente vía Docker y en **0 issues** en todo el repo — la config `.golangci.yml` (v1) era incompatible con `go 1.25.0` del `go.mod` (ninguna imagen v1.x de golangci-lint, ni compilada desde código fuente, soporta ese toolchain). Se migró la config a formato v2 (`golangci-lint migrate`) y se repinó `Makefile:lint-docker` a `golangci/golangci-lint:v2.12.2` (compilado con go1.26.2). Se corrigió el backlog completo expuesto al arrancar la herramienta (68 issues: `revive` — comentarios doc en símbolos exportados de `users`/`decks`/`statistics`/`sync`/`common`, `godot`, `gosec` — DSN de dev local y log de `port` marcados con `//nolint` justificado, `mnd`, `err113`/`errorlint`, `gocritic` `exitAfterDefer` en `main.go` refactorizado a patrón `run() error`, stutter rename `sync.SyncRequest/Response` → `sync.Request/Response`). Los 2 TODO intencionales de `games`/`game-actions` (lógica diferida a fase de refinamiento) quedan con `//nolint:godox` justificado en vez de eliminarse.
- [x] Wiring real de los `service.go` a sus `Queries` generadas por sqlc — **completo**: `users`, `auth`, `decks`, `games`, `game-actions`, `playgroups` y `statistics` están todos conectados de verdad a Postgres, sin módulos dummy pendientes. `decks` incluye ownership check (`GetDeck`/`DeleteDeck` devuelven 404 si el deck no es del usuario autenticado); `games`/`game-actions` son el motor de partida completo; `playgroups` auto-une al creador y acota listado/detalle a membresía; `statistics` recalcula de verdad al finalizar cada partida (ver Stage 1 y 7 para el detalle de cada uno)
- [x] Manejo de errores consistente (mapear errores de dominio a códigos HTTP de forma uniforme en todos los módulos) — ya implementado (TASKS.md nunca se había actualizado): `internal/common/errors.go` define `DomainError` + `MapError` + un `ErrorHandler` global de Fiber (wireado en `cmd/api/main.go`), y los 9 módulos del backend llaman `common.MapError(err)` de forma uniforme en su `handler.go`. **Corregido en el mismo cambio que agrega resiliencia a Moxfield** (ver Stage 8): `ErrorHandler` filtraba el string crudo de cualquier error interno no mapeado al cliente en todos los entornos (sin logging real del lado servidor) — ahora devuelve un mensaje genérico y loguea el error real
- [x] Rate limiting en endpoints de auth — ya implementado (TASKS.md nunca se había actualizado): `cmd/api/main.go` (`newAuthRateLimiter`) usa `gofiber/fiber/v2/middleware/limiter`, 20 req/min por IP, aplicado a `POST /auth/login`, `/auth/google`, `/auth/refresh` y `/auth/register`. Limitación conocida y ya documentada en el propio código: contador en memoria, válido para una sola instancia (ver ADR-0010); con réplicas habría que mover a un Storage compartido (Redis)
- [ ] Registrar ADRs en `docs/decisions/` a medida que se tomen decisiones técnicas nuevas — práctica iniciada 2026-07-26 (ver ADR-0001 a 0004 en Stage 0), mantenerla para decisiones futuras
- [x] Limpieza de carpetas vacías/residuales: `docker/`, `scripts/` (2026-07-27) — verificado que ninguna de las dos existe en el checkout ni tiene historial en `git log --all --diff-filter=A -- docker/ scripts/` (Git nunca trackeó un directorio vacío bajo esas rutas). La "limpieza" real era de documentación: quitadas las dos líneas del diagrama de árbol de `README.md` que las listaban como "(vacío por ahora)"
- [x] Dependabot (2026-07-27): `.github/dependabot.yml` creado con las 4 fuentes de dependencias del repo, actualizaciones semanales — `gomod` (`/backend`), `npm` (`/web`), `gradle` (`/android`, apunta a la raíz del proyecto Gradle donde vive `settings.gradle.kts`, no a `/android/app`) y `github-actions` (`/`, escanea `.github/workflows/` automáticamente)
- [x] Revisar los workflows de GitHub Actions para que un cambio que solo toca documentación no dispare las validaciones de backend/Android/web (2026-07-27) — auditados los 4 workflows job por job: un PR que solo toca `docs/**`/`*.md` de raíz ya no disparaba nada de `backend-ci.yml`/`android-ci.yml`/`web-ci.yml` (todo job downstream ya tenía su `if: needs.changes.outputs.<x> == 'true'` correcto). **Gap real encontrado y corregido** (ya había pasado en la práctica): el filtro de cada workflow (`backend/**`, `android/**`, `web/**`) también matchea los README propios de esos directorios (`backend/README.md`, `web/README.md`), así que un cambio de *solo* el README seguía disparando toda la batería pesada — para backend, build + `go test -race` contra Postgres real + migraciones + lint + `sqlc generate`. Arreglado en los 3 workflows: `dorny/paths-filter` con `predicate-quantifier: 'every'` + patrones `!*/README.md`/`!*/**/*.md` para excluir Markdown del filtro de código, separando el patrón del propio archivo de workflow en un filtro aparte (`backend_workflow`/`android_workflow`/`web_workflow`, combinado con `||` en cada `if`) porque bajo `'every'` dos rutas mutuamente excluyentes no pueden convivir en la misma lista de patrones. `docs-ci.yml` no se tocó (sus filtros ya son `docs/api/**`/`docs/database/**`, sin este problema)
- [ ] Actualización y simplificación de la documentación de `docs/` (agregado 2026-07-27): auditar y reducir duplicación/extensión entre ROADMAP, TASKS, ADRs y diagramas (hoy hay bastante redundancia acumulada entre revisiones) sin perder la trazabilidad histórica de decisiones ya tomadas
- [ ] Definir stack de infraestructura de despliegue (agregado 2026-07-27) — evaluar opciones y documentar la decisión final como ADR en `docs/decisions/`. Propuesta inicial (Opción 1, ver [ROADMAP.md](ROADMAP.md#infraestructura-de-despliegue-pendiente-de-decisión)): PaaS moderna/serverless — frontend en Vercel/Cloudflare Pages, backend en Fly.io/Render, base de datos en Neon/Supabase (Postgres serverless); recomendada para MVP rápido y coste cero. Faltan comparar alternativas antes de decidir

---

## Orden de trabajo sugerido

1. ~~**Higiene rápida**: ADRs mínimos de lo ya decidido~~ — hecho (ADR-0001 a 0010). `docker-compose.yml` ya centralizado en la raíz (ver Stage 1); `docker/`/`scripts/` en sí siguen vacíos y sin uso decidido.
2. ~~**Auth real** (Stage 1): JWT + bcrypt + middleware~~ — hecho (email/password + Google). Pendiente solo el paso manual externo: crear las credenciales OAuth en Google Cloud Console y setear `GOOGLE_CLIENT_ID`.
3. ~~**Conectar los servicios a la base de datos real** (Transversal): sacar los dummies de `users`, `decks`, `games`, `game-actions`, `playgroups`, `statistics`~~ — hecho, backend 100% wireado.
4. ~~**Estadísticas reales** (Stage 7): recálculo al finalizar partida~~ — hecho.
5. **Completar el contrato OpenAPI** (Stage 3) para que coincida con lo implementado — hecho el wiring de requests/responses de los módulos nuevos; sigue pendiente la paginación cursor-based.
6. ~~**Android: auth real (Stage 4-5)**: Credential Manager + Google, `POST /auth/*`, interceptor de sesión, persistencia con DataStore~~ — hecho 2026-07-27 (`LoginViewModel`, `NetworkModule`, `SessionManager`, `AuthInterceptor`/`AuthAuthenticator`); pendiente el paso manual externo (Web Client ID de Google Cloud) para probar el flujo de Google end-to-end. `CommanderApi.kt` real (decks/games/etc.) y las capas de dominio/repositorio quedaron resueltas/decididas más abajo en las revisiones siguientes (ver Stage 4 y 5) — ya no son la mayor brecha del proyecto. **Registro por email/password agregado 2026-07-30** (`RegisterScreen`/`RegisterViewModel`, ver Stage 4): Android sólo tenía login hasta esa fecha.
7. **Websocket** (Stage 6) una vez el flujo síncrono funciona de punta a punta — diseño ([ADR-0005](../decisions/0005-websocket-protocol.md)) y servidor (`internal/websocket/`) ya hechos; falta el cliente Android.
8. **Integración Moxfield** (Stage 8) — es la pieza más aislada, puede ir en paralelo o al final.
9. **Tests + CI** — idealmente no se dejan para el final; introducir tests a medida que se reemplazan los stubs en el punto 3 evita tener que rehacerlos después.
